# Журнал разработки — AniMori Desktop

> Ветка: `desktop-dev-1` (бывшая `desktop-dev`, стабильная версия этапа 1)
> Последняя ревизия, проверенная сборкой: `16de863` (35 модулей, 267.83 kB)
> Текущий HEAD: `0927dd6`
> Канонический источник поведения: монолит `animori.user.js` v1.9.1 (4658 строк)
> Этап 1 закрыт 30 июля 2026 года подтверждением разработчика.
> Работа по этапу 2 ведётся в ветке `desktop-dev-2` с отдельным журналом.

## Назначение

Цель Этапа 1 — разобрать монолитный юзерскрипт на модули TypeScript с нулевой регрессией
поведения. Никакого Vue, Tauri и Bridge на этом этапе нет — только разборка.

Правила ведения журнала: пишем решения и причины, а не пересказ диффа. Строки монолита
указаны приблизительно. Обновляется по требованию, а не после каждого коммита.

История до `cf8a431` велась подробно, поблочно (20 разделов, 51 КБ). Та версия целиком
лежит рядом: `docs/DEVELOPMENT_LOG_BIG.md` (тот же blob, что был в `cf8a431`), и остаётся
доступной командой `git show cf8a431:docs/DEVELOPMENT_LOG.md`. Здесь оставлено то, что нужно
для продолжения работы: решения, отступления от монолита, дефекты, риски и состояние этапа.

## 1. Каркас сборки

Vite 5.4.21 + `vite-plugin-monkey`, вход `src/main.ts`, вывод `dist/animori.user.js`
и `dist/animori.meta.js`. `minify: false`, `target: esnext`, alias `@` → `./src`,
`scss.api: 'modern-compiler'`, `define.__ANIMORI_PLATFORM__`.

Метаданные юзерскрипта перенесены 1:1: version `2.0.0` вместо `1.9.1`, `@match` только
AniList и `shikimori.io`, `@grant` из пяти разрешений, `@connect` без добавлений. Лишние
разрешения не добавлять: любое расширение `@grant` или `@connect` — повод для перепроверки
модераторами Greasy Fork.

Стиль кода: Prettier без точек с запятой, одинарные кавычки, `printWidth: 100`.
Каждая итерация закрывается прогоном `npx tsc --noEmit` и `npx vite build` на стороне
разработчика; без успешной сборки итерация не считается закрытой.

## 2. Структура кодовой базы

```
src/core/      accent, constants, custom-links, db, dictionary, lifecycle, settings, types
src/api/       anilist, anime365, animethemes, dictionary, shikimori, shikimori-people, titles
src/utils/     dom, logger, name-match
src/features/  exporter, scanner, translator, media, search, ui
```

Ключевые решения ядра:

- **Настройки** — единый объект `settings` с `loadSettings()`/`saveSetting()`. Все `GM_getValue`
  собраны в одном модуле: на Этапе 3 чтение станет асинхронным (РИСК №1), и править
  придётся только его. `loadSettings()` вызывается первым шагом `bootstrap()`.
- **Логгер** — ядро (`utils/logger.ts`) не знает о своём UI: представление подключается
  через `registerLogSink()`. На Этапе 2 UI заменится на `LoggerModal.vue`, ядро останется.
- **IndexedDB** — `DB_MIGRATIONS` с идемпотентными шагами вместо цепочки `if (oldVersion < N)`.
  Версии 1..5 консолидированы в шаг 5. Модуль переезжает на Tauri без изменений.
- **Словарь** — три слоя (локальный, удалённый, пользовательский) собираются
  `rebuildDictionary()`; приоритет у пользовательского. Перескан перевода после правки
  словаря — через `registerRetranslateCallback()`, чтобы словарь не зависел от переводчика.
- **Акценты** — `AM_ACCENTS` с пресетами `site`/`sakura`/`mono`/`catppuccin`, применение только
  внутри `.am-accent-scope`, чтобы не красить интерфейс сайта.
- **Панель действий** — `registerActionButton()` + `ACTION_ORDER`, порядок кнопок задаётся
  данными, а не порядком вызовов.
- **Медиа-страница** — `registerMediaWidget()` и единый `MediaContext`. Виджеты не знают
  друг о друге; данные AniList и Shikimori грузятся один раз на тайтл.
- **Вставка HTML** — только через тег `html` с автоэкранированием и явным `rawHTML()`
  там, где разметка доверенная.

## 3. Асинхронные API

- **AniList** — `anilistQuery()`, токен из `AL_TOKEN`, пауза по 429 через `pauseAniList()`.
- **Shikimori** — `fetchShiki()` с перебором зеркал `SHIKI_DOMAINS`; возвращает домен, с которого
  пришёл ответ, иначе фоллбэк-ссылки ведут на недоступное зеркало.
- **Anime365** — самоотключение после `ANIME365_FAIL_LIMIT` подряд сбоев, троттлинг 180 мс.
- **Переводчик** — три очереди (`MED2`, `CHR2`, `STF3`), пакеты 40 и 10, дебаунс 300 мс.
  Наблюдатель мутаций один на всё приложение; сторонние модули подписываются через
  `registerMutationHook()`. Защита от рекурсии — класс `am-notr` (РИСК №4).

## 4. Функциональные блоки

| Блок | Модуль | Суть |
| --- | --- | --- |
| Сканер | `features/scanner` | сверка списков Shikimori и AniList, модальное сравнение |
| Экспортёр | `features/exporter` | выгрузка Shikimori → AniList, работает только на Shikimori |
| Перевод | `features/translator` | интерфейс, названия, персонажи, стафф |
| Медиа | `features/media` | плеер, рейтинги, франшиза, темы, внешние ссылки |
| Поиск | `features/search` | русский поиск в шапке и захват словаря по выделению |
| UI | `features/ui` | панель действий, настройки, редактор словаря, оверлей логгера |

UI настроек и логгера перенесён императивно, 1:1 с монолитом. Решение сознательное:
этот код целиком умирает на Этапе 2 вместе с `SettingsModal.vue` и `LoggerModal.vue`,
поэтому вкладываться в его архитектуру нет смысла, а расхождения с 1.9.1 недопустимы.

## 5. Итерации этапа 1

| Итерация | HEAD | Содержание | Модули | Размер |
| --- | --- | --- | --- | --- |
| база | `7ffc666` | перенос ядра, API, сканера, экспортёра, перевода, медиа | — | — |
| 1 | `c7cef2f` | панель действий, фикс `bootstrap()`, уборка служебных выписок | 30 | 198.24 kB |
| 2+3 | `ece6a72` | русский поиск и захват словаря | 32 | 212.17 kB |
| 4 | `cf8a431` | UI настроек, редактор словаря, UI логгера | 34 | 265.34 kB |
| 5 | `16de863` | SPA-жизненный цикл, сборщик мусора, фикс статистики БД | 35 | 267.83 kB |
| 5а | `0540aef` | русский поиск не зависит от перевода интерфейса | 35 | — |
| 6 | `395970a` | приёмка этапа: сверка `bootstrap()` и всех `GM_*`, чистка | 35 | — |
| 7 | `0927dd6` | версия 2.0.0, фиксы регрессий переводчика по итогам проверок | 35 | — |

### Итерация 4 — UI настроек, словаря и логгера

Коммиты `aaa7561` (`features/ui/logger-ui.ts`, 486 строк), `4aac544`
(`features/ui/settings.ts`, 981 строка), `cf8a431` (подключение в `bootstrap()`).

Оверлей логгера: семь фильтров, поиск, группировка однотипных записей, JSON-вьюер,
счётчик непрочитанных, дамп состояния, копирование и скачивание. Отличия от монолита:
поиск идёт также по пути и деталям записи (сериализация в `try/catch`, чтобы циклическая
структура не роняла рендер); текстовый дамп собран в одну функцию вместо двух
расходящихся копий; дамп состояния собирается только из публичных геттеров, из-за
чего из него ушло поле `currentMediaId` (приватное состояние медиа-модуля).

Панель настроек: восемь вкладок, 13 переключателей и 3 поля доменов описаны таблицами,
а не повторяющимися блоками кода; типы ключей выведены из `AniMoriSettings` через mapped type,
поэтому новая настройка без правки таблицы не соберётся.

Отступление: импорт словаря из файла вызывает `upsertUserDictEntry()` вместо приватного
`amRetranslate()` монолита; ручной `rebuildDictionary()` убран, так как `upsert` делает его сам.

Квирк, перенесённый осознанно: оверлей логгера не помечен `am-notr`, как и в 1.9.1.

### Итерация 5 — SPA-жизненный цикл

Коммиты `849f835` (`features/media/index.ts`), `83ad7cc` (`core/db.ts`), `c4302e8`
(`core/lifecycle.ts`), `16de863` (`main.ts`), `0540aef` (`features/search/index.ts`).

Закрыты четыре дыры, возникшие именно при разборке монолита:

1. **Перехват навигации отсутствовал вовсе.** Новый `core/lifecycle.ts` ставит обёртки над
   `history.pushState` и `history.replaceState`, слушает `popstate` и держит страховочный
   пулинг `location.href` каждые 800 мс. Модуль не знает ничего о виджетах и селекторах:
   он лишь сообщает о смене адреса. В монолите та же логика была вмешана в `init()`
   и сама чистила DOM по списку классов. Отличие в лучшую сторону: обработчик обёрнут
   в `try/catch` — мы подменили сайту History API, и непойманное исключение вылетело бы
   наружу прямо из вызова `pushState`, то есть наш баг ломал бы навигацию AniList.
2. **Сборщик мусора не вызывался.** `runGarbageCollector()` был перенесён, но его вызов
   потерялся: кэш Shikimori рос бесконечно, а `CACHE_TIME` в 90 дней ни на что не влиял.
   Восстановлен запуск через 15 с после старта.
3. **Кнопка плеера не гаслась при уходе со страницы.** `#ru-player-btn` живёт в общей
   панели действий, а не в разметке виджета, поэтому `cleanupSelectors` её не снимал и кнопка
   оставалась висеть на списках и в профиле. Оверлей `#ru-player-overlay` при этом
   удаляется целиком, поэтому отдельно обнулять `src` у iframe не требуется — здесь порт
   чище монолита, который оставлял оверлей в DOM навсегда.
4. **Счётчик тем всегда показывал 0.** Темы кэшируются под префиксом `THEMES2_`, а `getDbStats()`
   искал `THEMES_`. Дефект монолита, затрагивал только статистику, сам кэш работал.

Для связи жизненного цикла с медиа-модулем добавлен экспорт `refreshMediaPage()` — аналог
`injectMediaExtensions()` монолита. Наблюдателя мутаций для этого недостаточно: переход
между однотипными страницами может не менять разметку сайдбара вообще.

Квирк поиска (`0540aef`): монолит опознавал поле поиска сравнением placeholder со строкой
'Поиск в AniList', то есть с результатом работы своего же переводчика. При
`translateInterface: false` русский поиск молча не работал, и любая правка формулировки
в `dictionary.json` его бы сломала. Теперь поле опознаётся по смыслу placeholder'а:
упоминание AniList плюс глагол поиска на любом из двух языков. Проверка оставлена узкой
сознательно: ложное срабатывание показало бы выпадашку поверх чужого поля ввода.
Это первое сознательное расхождение с поведением 1.9.1, утверждённое разработчиком.

### Итерация 6 — приёмка этапа

Кода почти нет: сверка порта с монолитом, чистка остатков, `395970a`.

**Сверка `bootstrap()` с хвостом `init()` (строки 4596-4658).** Порядок совпадает:
настройки → перехватчики ошибок → ранний выход на Shikimori (только экспортёр) → акцент →
регистрация кнопок и панель → `openDB()` → словарь → `initTranslator()` → `initSearch()` →
регистрация пяти медиа-виджетов и `initMedia()` → `initLifecycle()` → сборщик мусора.
Два отличия, оба осознанные: чистка DOM при смене роута перенесена из `init()` внутрь
медиа-модуля (`cleanupWidgets`), а `initTranslator()` вызывается всегда, тогда как флаги
перевода гасят только загрузку удалённого словаря — иначе вместе с переводом умирают
медиа-виджеты, которые живут на его наблюдателе мутаций.

**Аудит всех `GM_*` монолита.** Пять API: `GM_getValue` (31 вызов), `GM_setValue` (15),
`GM_xmlhttpRequest` (11), `GM_setClipboard` (5), `GM_addStyle` (2). Собран полный список
ключей хранилища — 26 штук — и каждый найден в порте:

- 19 ключей настроек и `am_accent` — в `core/settings.ts`, включая legacy-фоллбэк `set_titles`
  для пользователей, у которых ещё нет `set_title_primary`;
- `am_user_dict`, `am_custom_links`, `am_fav_translations`, `AL_TOKEN` — по своим модулям;
- `am_music_service` (строки 3356, 3448) — в `features/media/themes.ts` как `SERVICE_KEY`;
  порт добавил валидацию значения по списку сервисов, монолит доверял хранилищу вслепую;
- `CMP_IGNORE` и `SHIKI_LOGIN` (строки 687, 693, 1181, 1259) — в `features/scanner/index.ts`.

Потерянных ключей нет. `GM_setClipboard` используется в `utils/dom.ts` (`amCopy`) и в
редакторе словаря; `GM_xmlhttpRequest` — во всех модулях `api/`.

**Грант `GM_addStyle` решено оставить.** В монолите он вызывался напрямую (строка 3941),
в порте прямых вызовов нет — стили идут через `import './style.scss'`. Но CSS в бандл
встраивает `vite-plugin-monkey`, и он использует именно `GM_addStyle`, когда тот объявлен
в `@grant`. Снятие гранта не убрало бы разрешение, а переключило плагин на самодельную
вставку `<style>` — то есть изменило бы поведение вместо его сохранения. Пункт закрыт
как не требующий правки.

**Чистка.** `src/features/translator/.gitkeep` удалён (каталог давно занят четырьмя файлами).
Файла `src/_extracted_style.css` в дереве нет — служебная выписка была убрана ещё
в итерации 1, отдельного действия не потребовалось.

### Итерация 7 — ручные проверки и фиксы регрессий

Коммиты `92cbf94` (версия 2.0.0), `ab5da0b` и `0927dd6` (переводчик).

Проверки в браузере прошли по всему списку итерации 6 и вскрыли две регрессии порта,
обе в переводчике и обе с одной первопричиной — работа с текстом узла:

1. **Перевод сносил разметку карточек и виджетов** (`ab5da0b`). В `applyTranslation`
   фоллбэк `nameEl.textContent` затирал дочерние элементы, если прямого текстового узла
   в элементе не было: строки франшизы (`.franchise-node` из трёх дивов) теряли
   год, название и тип, а карточки избранного на профиле теряли обложку.
   Исправление: `writeText()` пишет только в текстовый узел, а `textContent` используется
   исключительно для элементов без дочерних элементов. Заодно `isTranslatableLink()`
   отсеивает ссылки без своего текста и без `.name` (обложки, плитки избранного),
   свой UI (`.am-notr`, `.animori-*`), и восстановлен `closest('.role')`
   вместо `classList.contains('role')`.
2. **Тултипы карточек переводились только при первом наведении** (`0927dd6`). AniList
   переиспользует один узел `.tooltip` для разных карточек, поэтому одноразовые маркеры
   его глушили. Восстановлено поведение монолита: `dataset.translated` и `dataset.queued`
   хранят id/ключ тайтла, а не `'1'`; `processMutations` снова дёргает `processTooltip`
   на `characterData` и на `childList` внутри `.tooltip` (строки 3016 и 3021 монолита);
   `processTooltip` сравнивает текущий текст с `dataset.ru` вместо опоры на маркер
   и ставит задачу через `force` — данные берутся из кэша, лишних запросов нет.

Сценарий с полностью выключенными флагами перевода — главный кандидат на баг по итогам
итерации 5 — проверен: медиа-виджеты живы, `openDB()` и `initTranslator()` выполняются.

## 6. Исправленные дефекты монолита

| Где | Суть | Коммит |
| --- | --- | --- |
| Ссылка авторизации AniList | адрес был сломан в 1.9.1, авторизация не работала | `4aac544` |
| Placeholder своей ссылки | тот же класс дефекта | `4aac544` |
| Счётчик тем в инспекторе БД | искал `THEMES_` вместо `THEMES2_` | `83ad7cc` |
| Зависимость поиска от перевода | поле опознавалось по переведённому тексту | `0540aef` |

Регрессии самого порта, найденные и закрытые по ходу:

- самодельная кнопка сравнения с инлайн-стилями вместо общей панели действий;
- `amSetAccent()` не вызывался при старте: сохранённый пресет акцента игнорировался;
- `if (!needTranslator) return` стоял до `openDB()`, из-за чего при полностью выключенном
  переводе умирали все медиа-виджеты. Сценарий проверен в браузере в итерации 7;
- `applyTranslation` затирал разметку франшизы и карточек избранного (`ab5da0b`);
- тултипы карточек переводились только при первом наведении (`0927dd6`).

## 7. Инструментальные решения и грабли

- **Абсолютные адреса собирать только конкатенацией.** Литерал схемы внутри шаблонной
  строки дважды сворачивался при передаче файла в репозиторий и приводил к битым ссылкам.
  После отправки файлы с URL перечитывать с ветки.
- **Журнал пишется компактно.** У API нет операции «дописать в файл»: любое обновление —
  передача всего файла целиком. При 51 КБ отправка рвалась и стоила десятков минут,
  поэтому журнал переписан в сжатом виде, а поблочная история вынесена в отдельный файл.
- **Выгружать файлы из истории git только через `cmd /c` или `git checkout <sha> -- <path>`.**
  В PowerShell 5.1 и `>`, и `Out-File` прогоняют поток через текстовый декодер: UTF-8
  с кириллицей разваливается, файл вырастает вдвое и попадает в репозиторий испорченным.
- **Поиск по коду в API неработоспособен на этой ветке** — всегда отдаёт пустой результат.
  Работают только чтение конкретных файлов и grep по локальной копии монолита.
- **Файлы отправляются по одному, последовательно** — параллельная отправка даёт гонку по ref.
- **Удаление и переименование ветки делается разработчиком вручную.** У интеграции есть
  создание ветки, но нет удаления: «переименование» — это создание новой ветки от того же
  коммита плюс ручное удаление старой.
- **Пуш идёт прямо в рабочую ветку** без feature-веток и PR; проверка сборки — локально
  на стороне разработчика после каждой итерации.

## 8. Риски

Из `AUDITION.md`, актуальны на Этапах 2-4:

1. Синхронный `GM_getValue` против асинхронного store в Tauri — всё чтение настроек
   уже изолировано в `core/settings.ts`.
2. Cookies Shikimori не видны Rust-клиенту — авторизованные запросы потребуют отдельного
   решения в Bridge.
3. React уничтожает вставленные узлы — виджеты обязаны быть идемпотентными.
4. Рекурсия мутаций переводчика — свою разметку помечать `am-notr`.
5. WebKitGTK без GStreamer не воспроизведёт H.264/AAC — касается плеера на Linux.
6. Логгер держит до 1000 записей на тип, а оверлей рендерит их без виртуализации —
   до шести тысяч узлов в DOM. Нужен кольцевой буфер 300-500 или виртуальный список
   при переводе на Vue.

## 9. Состояние этапа 1 — ЗАВЕРШЁН

Этап закрыт 30 июля 2026 года подтверждением разработчика по итогам ручных проверок.

Разборка и порт:

- [x] каркас сборки, метаданные, стили
- [x] ядро: настройки, логгер, IndexedDB, словарь, акценты, свои ссылки, типы
- [x] API: AniList, Shikimori, Anime365, AnimeThemes, Shikimori People, словарь
- [x] сканер и экспортёр
- [x] переводчик: интерфейс, названия, персонажи, стафф, очереди
- [x] медиа-страница: плеер, рейтинги, франшиза, темы, внешние ссылки
- [x] панель действий
- [x] русский поиск и захват словаря
- [x] UI настроек `#am-panel` и редактор пользовательского словаря
- [x] UI логгера через `registerLogSink()`
- [x] SPA-жизненный цикл и сборщик мусора кэша
- [x] русский поиск не зависит от настроек перевода
- [x] `bootstrap()` сверен с `init()` монолита
- [x] все `GM_*` и 26 ключей хранилища сверены, потерь нет
- [x] грант `GM_addStyle` проверен и оставлен осознанно
- [x] остатки каркаса вычищены

Ручные проверки в браузере:

- [x] сканер, экспортёр, переводчик, плеер, рейтинги, франшиза, темы, внешние ссылки
- [x] поиск и захват словаря, настройки (вкладки, акценты, импорт/экспорт словаря,
      ссылка авторизации AniList), логгер (фильтры, группировка, дамп состояния)
- [x] SPA-переходы: медиа → список → медиа, кнопки Назад/Вперёд, исчезновение кнопки плеера
- [x] сценарий с полностью выключенными флагами перевода — виджеты живы
- [x] русский поиск при `translateInterface: false`

Найденные при проверках регрессии переводчика закрыты в `ab5da0b` и `0927dd6`
(см. итерацию 7). Дальнейшие находки чинятся по мере появления в ветке `desktop-dev-2`.

## 10. Следующий шаг

Этап 2 (RM2) ведётся в ветке `desktop-dev-2`, отделённой от этой на коммите `0927dd6`.
Эта ветка остаётся стабильной версией этапа 1 и точкой отката; журнал этапа 2 живёт
в своей ветке отдельным файлом, чтобы история этапов не смешивалась.

Содержание этапа 2: `vue-mounter.ts`, `LifecycleManager` на базе `core/lifecycle.ts`,
замена `features/ui/settings.ts` на `SettingsModal.vue` и `features/ui/logger-ui.ts`
на `LoggerModal.vue`, затем `ScannerModal.vue` и `ActionPanel.vue`. Императивный UI
из итерации 4 при этом удаляется целиком — на то и был расчёт, когда его переносили
дословно.

При переходе сразу закрыть РИСК №6: кольцевой буфер логов или виртуальный список
в `LoggerModal.vue`.
