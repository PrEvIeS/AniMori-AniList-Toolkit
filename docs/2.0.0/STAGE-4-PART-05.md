# Журнал разработки AniMori — часть 5

## 0. Зачем новая часть

Часть 4 (`STAGE-4-PART-04.md`, около 39 КБ) заморожена на разделе 7. Порог
заведения новой части — 45–50 КБ, и часть 4 к нему подошла вплотную. Причина
порога прежняя и записана в части 4, пункт 0: запись журнала идёт полной
перезаписью файла, поэтому крупный журнал однажды обрывается на середине.
Это уже случалось дважды.

Правило прежнее: **старые части задним числом не правятся**. Разделы ниже
в нескольких местах поправляют то, что записано в плане `RM4.md`
и в `MANIFEST.md`. Сами эти документы остаются как есть — они замороженные
планы, а не описание текущего состояния. Расхождения фиксируются здесь.

### 0.1 Журналы переименованы

Часть 4 в разделе 0 ссылается на файлы `docs/DEVELOPMENT_LOG.md`,
`DEVELOPMENT_LOG_2.md` и `DEVELOPMENT_LOG_3.md`. **Этих имён больше нет.**
Вся документация 2.0.0 собрана в `docs/2.0.0/`, журналы переименованы
по этапам. Соответствие такое:

| Было | Стало |
| --- | --- |
| `docs/DEVELOPMENT_LOG.md` из ветки dev-1 | `docs/2.0.0/STAGE-1.md` |
| `docs/DEVELOPMENT_LOG_BIG.md` из ветки dev-1 | `docs/2.0.0/STAGE-1-FULL.md` |
| `docs/DEVELOPMENT_LOG.md` из ветки dev-3 | `docs/2.0.0/STAGE-2-3.md` |
| `docs/DEVELOPMENT_LOG.md` этапа 4 | `docs/2.0.0/STAGE-4-PART-01.md` |
| `docs/DEVELOPMENT_LOG_2.md` этапа 4 | `docs/2.0.0/STAGE-4-PART-02.md` |
| `docs/DEVELOPMENT_LOG_3.md` этапа 4 | `docs/2.0.0/STAGE-4-PART-03.md` |
| `docs/DEVELOPMENT_LOG_4.md` этапа 4 | `docs/2.0.0/STAGE-4-PART-04.md` |

Нумерация частей внутри этапа 4 сохранена намеренно: части ссылаются друг
на друга словами «часть 2», «часть 3», и переименование в сквозную нумерацию
сделало бы эти ссылки ложными.

---

## 1. Реорганизация репозитория

### 1.1 Что сделано

Коммит `6dcafe4`, 17 файлов, 1768 добавлено, 4662 удалено.

Перед релизом владелец решил сделать `desktop-dev-4` единственной веткой
разработки десктопа, а ветки 1–3 удалить. Прежде чем удалять, вся
документация была собрана в одно место.

- Создан каталог `docs/2.0.0/`. Туда переехали четыре части журнала этапа 4,
  `MANIFEST.md`, `AUDITION.md` и планы `RM1.md`–`RM4.md` из `docs/refactoring/`.
  Все десять перемещений git распознал как переименования на 100 %, история
  файлов не порвалась.
- Три журнала прошлых этапов существовали только в истории удалённых веток.
  Они извлечены из соответствующих коммитов и положены рядом с остальными.
- Из корня удалён `animori.user.js` — собранный скрипт версии 1.9.1.
  Он больше не нужен: релизная сборка будет выкладываться в GitHub Releases.
  `dictionary.json` в корне **остаётся неприкосновенным** — браузерный скрипт
  тянет его напрямую с `raw.githubusercontent.com` из ветки `main`, и любое
  перемещение файла сломает перевод у действующих пользователей 1.9.1.
- Удалены три пустых `.gitkeep` из `src/api/`, `src/core/`, `src/utils/` —
  каталоги давно наполнены.

### 1.2 Проверка происхождения журналов

Перед извлечением потребовалось выяснить, что именно лежит в каждой ветке.
Здесь я ошибся и был поправлен владельцем.

Я утверждал, что журнал — один непрерывно растущий документ, и что размеры
30 → 77 → 87 КБ это последовательные состояния одного файла. **Неверно.**
Каждый этап заводил свой журнал с чистого листа; это видно по коммитам сброса
`20df6e60`, `4d8a5884`, `66e7df60`. Исключение ровно одно: между этапами 2 и 3
сброса не было, они делят один файл, который дорос с 77 412 до 86 868 байт
дописыванием. Поэтому отдельный архив вершины dev-2 не нужен — её содержимое
целиком входит в `STAGE-2-3.md`.

Отдельно проверено, что ветки 1–3 являются предками dev-4. История с фильтром
по пути показала, что коммиты `9a0c7260`, `5e2287a`, `46cbbf3` и `ead0df9`
достижимы из вершины dev-4. Цепочка линейная: dev-1 → dev-2 → dev-3 → dev-4.
Значит, удаление указателей веток не теряет ни одного коммита.

Здесь же снята моя более ранняя рекомендация ставить архивные теги
`archive/desktop-dev-{1,2,3}`. Она была выдана до проверки достижимости
и после проверки потеряла смысл: теги защищают от сборки мусора коммиты,
на которые больше никто не ссылается, а на эти ссылается сама dev-4.

### 1.3 Кодировка: как я испортил три файла

Перемещать файлы через API GitHub нельзя — в наборе инструментов нет операции
переименования, а перезаливка трёхсот килобайт журналов это ровно та операция,
которая однажды уже оборвалась на середине. Поэтому весь перенос был отдан
владельцу скриптом для консоли, а я его составил.

В скрипте извлечение исторических файлов было записано так:

```
git show <sha>:<path> | Out-File -Encoding utf8 <target>
```

**Это ломает кириллицу.** Windows PowerShell 5.1 не передаёт байты git
напрямую: он декодирует поток кодовой страницей консоли (cp866), получает
мусор, и уже мусор записывает в UTF-8. Простое перенаправление через `>`
портит точно так же. Я предупредил владельца только про BOM, а про перекодировку
самого потока не подумал. Три файла — `STAGE-1.md`, `STAGE-1-FULL.md`
и `STAGE-2-3.md` — уехали в `6dcafe4` нечитаемыми. Заодно git выдал три
предупреждения про замену CRLF на LF: перезапись через консоль подменила ещё
и переводы строк.

Правильный способ — не пропускать содержимое через консоль вообще:

```
git checkout <sha> -- <path>
git mv <path> <target>
```

`checkout` пишет байты в рабочий каталог сам, консоль в цепочке не участвует.
Предупреждения про CRLF при этом тоже исчезли.

Восстановление закоммичено как `6e39cd7`. Проверка сделана по размеру
объектов в индексе, а не на глаз: `git cat-file -s` дал 30 097, 51 161
и 86 868 байт — точное совпадение с исходными объектами. Совпадение размера
до байта означает, что восстановлено ровно то, что лежало в истории.

**Урок.** Любая передача содержимого файла через конвейер PowerShell — это
перекодировка. Если файл не ASCII, проверять надо не глазами по первым
строкам, а сравнением размера с исходным объектом.

### 1.4 Итоговый состав `docs/2.0.0/`

| Файл | Размер, байт | Что покрывает |
| --- | --- | --- |
| `MANIFEST.md` | 4 147 | Стратегия перехода 1.9.1 → 2.0.0, четыре шага |
| `AUDITION.md` | 6 702 | Реестр рисков рефакторинга |
| `RM1.md` | 5 964 | План шага 1: Vite и TypeScript |
| `RM2.md` | 7 494 | План шага 2: Vue 3 |
| `RM3.md` | 5 099 | План шага 3: слой Bridge |
| `RM4.md` | 11 458 | План шага 4: Tauri и Rust, предрелизный блок |
| `STAGE-1.md` | 30 097 | Этап 1, компактный журнал |
| `STAGE-1-FULL.md` | 51 161 | Этап 1, подробный журнал |
| `STAGE-2-3.md` | 86 868 | Этапы 2 и 3 |
| `STAGE-4-PART-01.md` | 72 203 | Этап 4, часть 1 |
| `STAGE-4-PART-02.md` | 52 503 | Этап 4, часть 2 |
| `STAGE-4-PART-03.md` | 53 026 | Этап 4, часть 3 |
| `STAGE-4-PART-04.md` | 38 574 | Этап 4, часть 4 |

Каталог `docs/refactoring/` расформирован.

### 1.5 Удаление веток

Ветки `desktop-dev-1`, `desktop-dev-2` и `desktop-dev-3` удалены владельцем
после того, как их содержимое было перенесено и проверено. Теги не ставились —
см. 1.2. Единственной веткой разработки десктопа остаётся `desktop-dev-4`.

АПИ GitHub, через который пишется журнал, удалять ветки не умеет: есть
создание, нет удаления. Команды выполнены локально.

---

## 2. CHANGELOG и README переписаны

Коммит `b3dbae6`.

Оба документа описывали версию 1.9.1 — одиночный юзерскрипт. Между ней
и 2.0.0 лежит вся работа четырёх этапов, о которой публичные документы
не знали ничего.

**CHANGELOG.** Добавлен раздел `[2.0.0]`. Всё, что относится только
к приложению, помечено явно, чтобы пользователь браузерной версии сразу видел,
что к нему не относится. Отдельным блоком выписаны известные ограничения:
только Windows, требуется WebView2, автообновления пока нет, преролл,
приезжающий с того же адреса, что и видео, фильтрами не снимается.

Раздела «Исправлено» в 2.0.0 нет намеренно. Дефекты A1–A14 найдены и закрыты
внутри самой 2.0.0, которая ни разу не выходила. Пользователь 1.9.1 их
не встречал, и рассказывать ему в публичной истории о поломках, которых
не было, значит вводить в заблуждение.

**README.** Появился блок выбора между скриптом и приложением с таблицей
сравнения, раздел «Только в приложении», два пути установки, требование
WebView2, раздел сборки из исходников и ссылка на `docs/2.0.0/`. Удалён
абзац про установку из корневого `animori.user.js` — файла больше нет.
Уточнено хранение токена: в скрипте это хранилище Tampermonkey,
в приложении — файл настроек.

### 2.1 Расхождения планов с реальностью

При сборе фактов для документов всплыли три места, где замороженные планы
описывают не то, что собрано.

**Платформы.** `MANIFEST.md` обещает приложение под Windows, macOS и Linux.
В `tauri.conf.json` собирается единственная цель `nsis`, то есть только
Windows. В `RM4.md` отказ от macOS и Linux зафиксирован правкой от 31.07,
в манифесте — нет.

**Движок блокировки.** Пункт 4.7Б в `RM4.md` описывает сетевой блокировщик
как крейт `adblock` от Brave с наборами фильтров EasyList и оставляет открытым
вопрос об атрибуции лицензии CC BY-SA. В `src-tauri/src/adblock.rs` нет
ни того, ни другого: списки хостов и примет собраны вручную по итогам живой
охоты 02.08. Никакой внешней лицензии в проекте нет, и атрибуция в README
не нужна. Это важно было поймать до публикации.

**Имя установщика.** `RM4.md` называет артефакт `AniMori_Setup.exe`. Tauri
именует NSIS-сборку иначе — по шаблону с версией и архитектурой,
`AniMori_2.0.0_x64-setup.exe`. Либо переименовывать файл в релизном
workflow, либо править README после первой боевой сборки. Решение пока
не принято, README сейчас обещает короткое имя.

Править сами планы задним числом не будем — они исторические документы.
Этот пункт и есть место, где расхождения записаны.

---

## 3. Автообновление: разбор механизма

Предрелизный блок `RM4.md` ставит автообновления пунктом П.2 и отмечает,
что разбор надо начинать с объяснения механизма, а не с кода. Ниже разбор.
Работы ещё не начаты; это план, а не отчёт.

Скрипта тема не касается вовсе: у него канал обновлений давно есть, Greasy
Fork рассылает новую версию по метаданным `update.greasyfork.org/scripts/572948`.
Речь только о десктопе.

### 3.1 Почему это надо решить до релиза

Апдейтер Tauri проверяет подпись установщика публичным ключом, **вшитым
в бинарник на момент компиляции**. Если 2.0.0 выйдет без ключа и без адреса
манифеста, то ни один установивший её пользователь не сможет обновиться
автоматически никогда — только скачать следующую версию руками. Пользователей
у нас пока ноль, так что цена ошибки сегодня минимальна и растёт с каждым днём
после релиза. Владелец выбрал полный вариант в составе 2.0.0.

### 3.2 Из чего состоит

- Два плагина: `tauri-plugin-updater` и `tauri-plugin-process`. Второй нужен,
  чтобы перезапустить приложение после установки.
- Пара ключей подписи, генерируется `npm run tauri signer generate`. Публичный
  идёт в `tauri.conf.json`, в `plugins.updater.pubkey`. Приватный и пароль
  к нему — в секреты репозитория, `TAURI_SIGNING_PRIVATE_KEY`
  и `TAURI_SIGNING_PRIVATE_KEY_PASSWORD`. В репозиторий приватный ключ
  не попадает ни при каких условиях, и копия его хранится отдельно: потеря
  ключа означает новую версию с новым публичным ключом и ручную переустановку
  у всех.
- Манифест `latest.json` со ссылкой на установщик и его подписью. Отдельный
  сервер не нужен: адрес вида `.../releases/latest/download/latest.json`
  всегда указывает на последний нечерновой релиз. Файл генерирует релизный
  workflow.
- Разрешения в `src-tauri/capabilities/default.json`: `updater:default`
  и `process:allow-restart`.

### 3.3 Порядок работ

Апдейтеру нечего проверять, пока автосборка не начнёт выкладывать установщик,
подпись и манифест. Значит, П.1 из `RM4.md` — не параллельная задача,
а обязательный первый шаг.

В workflow важен порядок команд сборки: `emptyOutDir: !isTauri` означает, что
юзерскриптовая сборка чистит `dist/`, а тауриная — нет. Сначала `build`,
потом `build:tauri`, иначе первая снесёт артефакты второй. Это уже записано
в части 4, пункт 7.1, и здесь повторено, потому что именно в CI ошибка
будет незаметной.

### 3.4 Подводные камни, замеченные заранее

- **Pre-release.** Релиз, помеченный предварительным, в `latest` не попадает.
  Апдейтер такое обновление просто не увидит.
- **Двойная версия.** Сейчас `2.0.0` записана и в `package.json`,
  и в `tauri.conf.json`. Tauri умеет читать версию из `package.json`, если
  в поле `version` указать путь к нему. Тогда при выпуске правится одно место,
  и невозможна рассинхронизация, при которой установленное приложение считает
  себя новее вышедшего обновления.
- **Режим установки NSIS.** Из трёх вариантов — тихо, с полосой прогресса,
  полный визард — для обновления разумен средний: пользователь видит, что
  процесс идёт, но ничего не нажимает.
- **Проверить можно только вживую.** Убедиться, что механизм работает,
  получится не раньше, чем будут опубликованы два релиза подряд.

---

## 4. Состояние дефектов

Без изменений с части 4. Открытыми остаются A9 и A10 (этап C), A13 числится
как невоспроизводимый.

---

## 5. Точка остановки

- Ветка `desktop-dev-4`, вершина после этой записи. Ветки 1–3 удалены,
  их история целиком достижима отсюда.
- Документация 2.0.0 собрана в `docs/2.0.0/`, кодировка исторических журналов
  восстановлена и проверена побайтово.
- CHANGELOG и README переписаны под две сборки.
- **Следующее — автообновление**, полный вариант, в составе 2.0.0. Порядок:
  сначала релизный workflow `.github/workflows/release.yml` с артефактами
  `animori.user.js` и установщиком NSIS, затем подпись, манифест и плагины.
- Открытые вопросы к владельцу, не блокирующие старт работ: окончательное имя
  файла установщика (см. 2.1) и нужен ли в `docs/2.0.0/` файл-указатель
  с описанием того, какой журнал что покрывает.
- Не сделано и остаётся в списке: этап C (A9, A10), проверка РИСКА №4
  из `AUDITION.md` в браузерной сборке, комментарий к `hideAds`
  в `src/core/settings.ts`, вычистка отладочных `diagnose()`, `DIAG_SELECTOR`,
  `DIAG_LIMIT` и `diagReported` из `features/adblock/index.ts`.
