# Чистка комментариев (2.0.1)

Сквозная задача, идёт параллельно этапу 5 и не привязана к его пунктам.
Заведена отдельным документом: в `STAGE-5-PART-06.md` она разрывала рассказ
про пункт 5.3.7 и всё равно переживёт этап.

## Зачем

Комментарии занимают около 40% веса исходников. Тяжёлые файлы труднее
править целиком, а пересказ кода словами устаревает раньше самого кода.

Правило — `docs/CODE-STYLE.md`. Чистка НЕ трогает код, только комментарии.
`README.md` и бейджи не трогаем. Ценное «почему» переезжает в `docs/`, а не
удаляется.

### Редакция правила

После чистки `lib.rs` правило ужесточено (`dc20194`): док-комментарий — 2 строки
вместо 3, запрет комментариев из двух абзацев, пустая строка `//` внутри
комментария запрещена. Повод — `NET_PROBE_SCRIPT`, вышедший на 11 строк через
четыре формально коротких абзаца.

Файлы, почищенные до `dc20194`, второй раз не перебираем: они попадут под новые
лимиты при ближайшей содержательной правке.

Про SCSS в правиле пока ничего нет — дописать по итогам партии по `style.scss`.

## Порядок итерации

1. Замерить вес файлов партии заново: оценки из очереди устаревают.
2. Почистить, ценное «почему» вынести в `docs/`.
3. Записать и перечитать файл целиком.
4. Прогнать локальные проверки (ниже).
5. Дописать партию в этот журнал с цифрами прогона.

### Проверка после итерации (PowerShell)

Рабочая машина — Windows, `grep` нет.

```powershell
git pull --ff-only origin "script&windows-dev"
git diff <до>..<после> -- <файл> |
  Select-String -Pattern '^[+-]' |
  Where-Object { $_ -notmatch '^[+-]\s*(//|/\*|\*)' -and $_ -notmatch '^(\+\+\+|---)' }
npx prettier --check <файл>
npm run typecheck
npm run build
```

Кавычки вокруг имени ветки обязательны: `&` уводит команду в фон.

Фильтр диффа ловит только строки, начинающиеся с маркера комментария. Хвостовые
комментарии вида `const x = 1 // пояснение` он покажет как правку кода — такие
смотрим глазами.

Для SCSS фильтр шумит, поэтому там сверяем число открывающих скобок до и после,
а в выводе сборки смотрим строку `dist/animori.css` (сейчас 46,76 КБ):

```powershell
(Select-String -Path "src/style.scss" -Pattern '\{' -AllMatches).Matches.Count
```

## Сделано

| Партия | Файл | Было | Стало | Коммит | Правило |
| --- | --- | --- | --- | --- | --- |
| 1 | `src/bridge/TauriBridge.ts` | ~25 КБ | 11 617 б | `c1ab4ac` | до `dc20194` |
| 1 | `src/bridge/IBridge.ts` | ~16 КБ | 8693 б | `11561da` | до `dc20194` |
| 1 | `src/main.ts` | ~23,5 КБ | 12 560 б | `d1e4c09` | до `dc20194` |
| 2 | `src/bridge/MonkeyBridge.ts` | 11 563 б | 8516 б | `80f5c28`, `b577a84` | до `dc20194` |
| 2 | `src/bridge/TauriProxyDiagnostics.ts` | 4173 б | 2544 б | `595c9bf` | до `dc20194` |
| 2 | `src/bridge/index.ts` | 2634 б | 1726 б | `da8e65c` | до `dc20194` |
| 3 | `src-tauri/src/lib.rs` | 27 098 б | 17 717 б | `876fa5a` | до `dc20194` |
| 4 | `src-tauri/src/proxy.rs` | 28 659 б | 16 664 б | `59d2d7b` | по новым |
| 5 | `src-tauri/src/adblock.rs` | 19 467 б | 13 245 б | `4045d89` | по новым |
| 5 | `src-tauri/src/proxy_guard.rs` | 10 992 б | 6025 б | `5a3ea87` | по новым |
| 6 | `src/features/translator/index.ts` | 47 065 б | 34 677 б | `8488822` | по новым |

Итого: 216 151 → 133 984 б, снято 82 167 б (−38%).

Сборка от чистки не худеет: комментарии в бандл не попадают. Разница
613,52 → 613,07 КБ после первой партии — единственный сдвиг, дальше вес стоит.
Цель задачи — исходники, а не сборка.

Попутно в `IBridge.ts` починена сломанная разметка док-комментария
`ProxyOutcome`: строка списка была без `*`.

### Партия 6: `translator/index.ts`

Самый тяжёлый файл проекта: 47 065 → 34 677 б (−12 388 б, −26%). Код не тронут.

Что снято:

- Шапка на 46 строк: пересказ конвейера по шагам, список отличий от монолита,
  два блока «ВАЖНО», разбор пункта 3.8 и ссылка на РИСК №4 — теперь 2 строки.
- Док-комментарии дефектов A6, A7, A8, A11, A12 — по 6–10 строк с историей
  и ссылками на журнал ч.3 §6.9–6.11, местами длиннее самого кода.
- Ссылки на номера строк монолита (2502–3060, 3016, 3021) и на коммит `1306f00`.

«Почему» вынесено в `docs/2.0.1/TRANSLATOR.md` (`3ed1944`, 7293 б). В коде оставлены
короткие «почему», без которых строка читается как ошибка: маркер после `dbGet`,
возврат остатка пачки при 429, приоритет персон, отсутствие пауз после 3.8,
нулевая рамка в `entryDistance`.

Проверка: `typecheck` чист, сборка зелёная — 88 модулей, `dist/animori.user.js`
660,95 КБ (gzip 156,58). Фильтр диффа дал пустой вывод. `prettier --check` по коду
прошёл, но потребовал правку `TRANSLATOR.md` (`23ce18f`, 8 строк выравнивания).

## Очередь

Замеры от 6 августа 2026, вершина `100a1ab`. Предыдущая редакция очереди брала
оценки на глаз и врала: `SettingsModal.vue` шёл как ~38,1 КБ против реальных
26 930 б, а `SettingsProxyCard.vue`, `shikimori-people.ts`, `player.ts` и `updater.rs`
в неё не попали вовсе.

| № | Цель | Вес | Файлов |
| --- | --- | --- | --- |
| 1 | `src/style.scss` | 46 173 б | 1 |
| 2 | `ui`: `SettingsModal.vue`, `SettingsProxyCard.vue`, `LoggerModal.vue` | 64 876 б | 3 |
| 3 | `ui`: `settings-state.ts`, `net-check.ts`, `reload.ts`, `links.ts`, `action-panel-state.ts` | 63 531 б | 5 |
| 4 | `ui`: остальное (вкладки настроек, панели, мелкие модули) | 58 522 б | 13 |
| 5 | `src/api` целиком | 119 074 б | 9 |
| 6 | `src/core` целиком | 91 364 б | 10 |
| 7 | `src/features/media` | 87 255 б | 8 |
| 8 | `src/features/scanner` | 61 928 б | 6 |
| 9 | `src/features/exporter` | 52 024 б | 5 |
| 10 | `src/utils` | 35 027 б | 4 |
| 11 | `src/features/adblock` | 32 430 б | 5 |
| 12 | `src/features/search` | 21 682 б | 2 |
| 13 | `src/features/translator`: `rules.ts`, `dom.ts` | 18 008 б | 2 |
| 14 | Хвосты: `src-tauri/src/updater.rs`, `src/vite-env.d.ts` | 8985 б | 2 |

Всего в очереди 760 879 б в 73 файлах. При темпе чистки −30% снимется ещё
около 230 КБ; пройдена примерно пятая часть работы.

Порядок выбран по весу каталога, а не отдельного файла: `src/features/ui` — это
четверть остатка. Каталогами чистить дешевле: в `src/api` девять файлов с
однотипными комментариями к запросам, одна проверка на всю пачку.

## Запись через API: что выяснилось

Порог касается текста, который отправляется, а не того, что лежит в репозитории.
Почищенный файл легче исходного, поэтому чистка тяжёлого файла через API
безопаснее, чем кажется.

Оценка порога в 25 КБ была заниженной. `translator/index.ts` записан целиком
одним вызовом на 34 677 б, перечитка подтвердила целостность. Значит, обрыв
`proxy.rs` был не чистым эффектом размера. Правило: размер сам по себе не
запрет, но контрольная перечитка после каждой записи обязательна: обрыв API
отдаёт как успех.

Файл, созданный через API, проходит `prettier --check` только случайно. Новые
документы прогоняем через `--write` до коммита, точечно по имени файла:
`npm run format` бьёт по корневым `*.md` и уже ломал `README.md` (`b2df266`,
откачено `a8f1451`).

Записи в одну ветку идут только последовательно: четыре параллельные дали 409.

## Долги по задаче

- Дописать в `docs/CODE-STYLE.md` абзац про SCSS по итогам партии `style.scss`.
- `docs/PROJECT-MAP-MODULES.md`: добавить ссылку на `docs/2.0.1/TRANSLATOR.md`.
