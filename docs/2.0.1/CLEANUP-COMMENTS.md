# Чистка комментариев (2.0.1)

Сквозная задача, идёт параллельно этапу 5 и не привязана к его пунктам.
Заведена отдельным документом: в `STAGE-5-PART-06.md` она разрывала рассказ
про пункт 5.3.7 и всё равно переживёт этап.

## Зачем

Комментарии занимают около 40% веса исходников. Тяжёлые файлы труднее
править целиком, а пересказ кода словами устаревает раньше самого кода.

Правило — `docs/CODE-STYLE.md`. Чистка НЕ трогает код, только комментарии.
`README.md` и бейджи не трогаем. Ценное «почему» переезжает в `docs/`, а не
удаляется.

### Редакция правила

После чистки `lib.rs` правило ужесточено (`dc20194`): док-комментарий — 2 строки
вместо 3, запрет комментариев из двух абзацев, пустая строка `//` внутри
комментария запрещена. Повод — `NET_PROBE_SCRIPT`, вышедший на 11 строк через
четыре формально коротких абзаца.

Файлы, почищенные до `dc20194`, второй раз не перебираем: они попадут под новые
лимиты при ближайшей содержательной правке.

Про SCSS в правиле пока ничего нет — что дописать, видно из партии 7.

В `.vue` под лимит шапки попадает и HTML-комментарий перед `<template>`.
Разделители секций в `<script setup>` и подписи блоков разметки остаются:
в файле на 500 строк разметки они единственный способ навигации.

## Порядок итерации

Работа идёт партиями по 2–3 файла, а не пофайлово.

1. Замерить вес файлов партии заново: оценки из очереди устаревают.
2. Прикинуть долю комментариев, а не только вес: см. урок партии 7.
3. Почистить, ценное «почему» вынести в `docs/`.
4. Записать и перечитать файл целиком — после каждого файла.
5. Дописать партию в этот журнал и перечитать его.
6. Прогнать локальные проверки одним набором на всю партию (ниже).

### Частота проверок

| Действие | Частота | Где выполняется |
| --- | --- | --- |
| Перечитка после записи | каждый файл | через API |
| Дифф-фильтр | раз в партию, списком файлов | локально |
| `prettier --check` | раз в партию, списком файлов | локально |
| `typecheck` и `build` | раз в партию | локально |
| Запись в журнал | раз в партию | через API |

Перечитка остаётся пофайловой: она ничего не стоит человеку и уже трижды
поймала брак записи в одной партии. `prettier --write` вслепую по `.md`
отменён: в партии 7 файл из API прошёл `--check` без правок.

### Проверка после итерации (PowerShell)

Рабочая машина — Windows, `grep` нет.

```powershell
git pull --ff-only origin "script&windows-dev"
git diff <до>..<после> -- <файл> |
  Select-String -Pattern '^[+-]' |
  Where-Object { $_ -notmatch '^[+-]\s*(//|/\*|\*)' -and $_ -notmatch '^(\+\+\+|---)' }
npx prettier --check <файл>
npm run typecheck
npm run build
```

Кавычки вокруг имени ветки обязательны: `&` уводит команду в фон.

Фильтр диффа ловит только строки, начинающиеся с маркера комментария. Хвостовые
комментарии вида `const x = 1 // пояснение` он покажет как правку кода — такие
смотрим глазами. В `.vue` он же покажет HTML-комментарии `<!-- -->`.

Для SCSS фильтр шумит, поэтому там сверяем число открывающих скобок до и после:

```powershell
(Select-String -Path "src/style.scss" -Pattern '\{' -AllMatches).Matches.Count
```

Сейчас скобок 309. Сборка в режиме `userscript` строку `dist/animori.css` не
печатает — стили инлайнятся в бандл; отдельный вес CSS (46,76 КБ) виден только
в десктопной сборке.

## Сделано

| Партия | Файл                                    | Было     | Стало    | Коммит               | Правило      |
| ------ | --------------------------------------- | -------- | -------- | -------------------- | ------------ |
| 1      | `src/bridge/TauriBridge.ts`             | ~25 КБ   | 11 617 б | `c1ab4ac`            | до `dc20194` |
| 1      | `src/bridge/IBridge.ts`                 | ~16 КБ   | 8693 б   | `11561da`            | до `dc20194` |
| 1      | `src/main.ts`                           | ~23,5 КБ | 12 560 б | `d1e4c09`            | до `dc20194` |
| 2      | `src/bridge/MonkeyBridge.ts`            | 11 563 б | 8516 б   | `80f5c28`, `b577a84` | до `dc20194` |
| 2      | `src/bridge/TauriProxyDiagnostics.ts`   | 4173 б   | 2544 б   | `595c9bf`            | до `dc20194` |
| 2      | `src/bridge/index.ts`                   | 2634 б   | 1726 б   | `da8e65c`            | до `dc20194` |
| 3      | `src-tauri/src/lib.rs`                  | 27 098 б | 17 717 б | `876fa5a`            | до `dc20194` |
| 4      | `src-tauri/src/proxy.rs`                | 28 659 б | 16 664 б | `59d2d7b`            | по новым     |
| 5      | `src-tauri/src/adblock.rs`              | 19 467 б | 13 245 б | `4045d89`            | по новым     |
| 5      | `src-tauri/src/proxy_guard.rs`          | 10 992 б | 6025 б   | `5a3ea87`            | по новым     |
| 6      | `src/features/translator/index.ts`      | 47 065 б | 34 677 б | `8488822`            | по новым     |
| 7      | `src/style.scss`                        | 46 173 б | 45 448 б | `da4823a`            | по новым     |
| 8      | `src/features/ui/SettingsProxyCard.vue` | 20 925 б | 14 786 б | `b34cbb0`, `f70d026` | по новым     |
| 8      | `src/features/ui/SettingsModal.vue`     | 26 930 б | 21 630 б | `6faa006`            | по новым     |
| 8      | `src/features/ui/LoggerModal.vue`       | 17 021 б | 16 804 б | `e77cea6`            | по новым     |

Итого: 327 200 → 232 652 б, снято 94 548 б (−29%).

Сборка от чистки не худеет: комментарии в бандл не попадают. Разница
613,52 → 613,07 КБ после первой партии — единственный сдвиг, дальше вес стоит.
Цель задачи — исходники, а не сборка.

Попутно в `IBridge.ts` починена сломанная разметка док-комментария
`ProxyOutcome`: строка списка была без `*`.

### Партия 6: `translator/index.ts`

Самый тяжёлый файл проекта: 47 065 → 34 677 б (−12 388 б, −26%). Код не тронут.

Что снято:

- Шапка на 46 строк: пересказ конвейера по шагам, список отличий от монолита,
  два блока «ВАЖНО», разбор пункта 3.8 и ссылка на РИСК №4 — теперь 2 строки.
- Док-комментарии дефектов A6, A7, A8, A11, A12 — по 6–10 строк с историей
  и ссылками на журнал ч.3 §6.9–6.11, местами длиннее самого кода.
- Ссылки на номера строк монолита (2502–3060, 3016, 3021) и на коммит `1306f00`.

«Почему» вынесено в `docs/2.0.1/TRANSLATOR.md` (`3ed1944`, 7293 б). В коде оставлены
короткие «почему», без которых строка читается как ошибка: маркер после `dbGet`,
возврат остатка пачки при 429, приоритет персон, отсутствие пауз после 3.8,
нулевая рамка в `entryDistance`.

Проверка: `typecheck` чист, сборка зелёная — 88 модулей, `dist/animori.user.js`
660,95 КБ (gzip 156,58). Фильтр диффа дал пустой вывод. `prettier --check` по коду
прошёл, но потребовал правку `TRANSLATOR.md` (`23ce18f`, 8 строк выравнивания).

### Партия 7: `style.scss`

46 173 → 45 448 б (−725 б, −1,6%). Партия закрыта одной правкой, а не полной
чисткой, и это осознанно.

В файле 309 правил и комментариев всего на ~1,3 КБ. Правилу противоречила только
шапка: 8 строк с двумя пустыми `//` внутри, ссылкой на строки монолита 3942–4207
и объяснением про `GM_addStyle`, которого в коде давно нет. Свёрнута в 2 строки,
всё существенное — запрет переименовывать `.amk-*` и `#am-panel` — осталось.

Остальные комментарии — односложные разделители секций вида
`/* ===== AniMori UI Kit ===== */`. Формально они правилу не противоречат, а по
сути помогают ориентироваться в 45 КБ стилей без единой функции. Оставлены.

Отдельно замечен комментарий с двумя мыслями (WARN и DEBUG в одной строке) и то,
что все комментарии в файле написаны через `/* */`, поэтому попадают в
скомпилированный CSS. Ни то, ни другое не тронуто: первое не окупает отдельного
прогона, второе меняет вывод сборки и требует решения (см. долги).

Проверка: скобок 309 до и после, `prettier --check` чист, сборка зелёная,
`dist/animori.user.js` 660,95 КБ (gzip 156,58) — без изменений.

**Урок.** Файл попал в очередь первым по весу, а дал меньше процента. Вес файла
не предсказывает отдачу: у `.scss` и `.vue` объём делают правила и разметка, а не
комментарии. Дальше очередь строим по доле комментариев.

### Партия 8: ui-модалки

Три файла одной партией: 64 876 → 53 220 б (−11 656 б, −18%). Код не тронут.

Разброс внутри партии оказался выше, чем между партиями:

| Файл | Снято | Доля |
| --- | --- | --- |
| `SettingsProxyCard.vue` | 6139 б | −29% |
| `SettingsModal.vue` | 5300 б | −20% |
| `LoggerModal.vue` | 217 б | −1,3% |

Что снято в `SettingsProxyCard.vue`: шапка на 27 строк и док-комментарии по
6–10 строк у `check()`, `save()`, `load()` и полей формы, ссылки на «Пункт 5.3»
и «Пункт 5.3.6». В `SettingsModal.vue`: шапка на 40 строк с перечислением пунктов
плана 2.2, 2.8, 2.10, 3.7.2, 4.3 и историей итераций 5.1 и 5.2, десятистрочный
док `onClearCache()`, разбор альтернативы `confirm()`.

«Почему» вынесено в `docs/2.0.1/SETTINGS-UI.md` (`97f7b42`, 11 751 б): общие правила
панели, два клика вместо `confirm()`, перезагрузка через мост, два шага проверки
прокси, безопасность `v-html` в логгере.

`LoggerModal.vue` был почти чист изначально: в нём только разделители секций и
короткие пояснения. Поправлены два места: блок `jsonView` с пустой строкой `//`
внутри и сравнение с императивным кодом в `onDumpState()`.

**Урок.** Отдача в `.vue` зависит не от веса файла, а от того, есть ли в нём
шапка-летопись с пунктами плана. Где такая шапка есть, чистка даёт 20–30%; где
нет — единицы процента. Сортировать очередь стоит по наличию таких шапок.

Второй урок — про запись, см. ниже: в одной партии подряд попались три дефекта
генерации текста.

## Очередь

Замеры от 6 августа 2026. Порядок ниже — по весу каталога, и этот критерий уже
дважды подвёл: сначала оценки брались на глаз (`SettingsModal.vue` шёл как
~38,1 КБ против реальных 26 930 б, четыре файла не попали вовсе), потом первая
позиция дала −1,6% (партия 7).

Перед следующей партией очередь пересобирается по доле строк-комментариев.
До тех пор таблица годится как опись остатка, но не как порядок работ.

| №   | Цель                                                                                        | Вес       | Файлов |
| --- | ------------------------------------------------------------------------------------------- | --------- | ------ |
| 1   | `ui`: `settings-state.ts`, `net-check.ts`, `reload.ts`, `links.ts`, `action-panel-state.ts`  | 63 531 б  | 5      |
| 2   | `ui`: остальное (вкладки настроек, панели, мелкие модули)                                   | 58 522 б  | 13     |
| 3   | `src/api` целиком                                                                            | 119 074 б | 9      |
| 4   | `src/core` целиком                                                                           | 91 364 б  | 10     |
| 5   | `src/features/media`                                                                         | 87 255 б  | 8      |
| 6   | `src/features/scanner`                                                                       | 61 928 б  | 6      |
| 7   | `src/features/exporter`                                                                      | 52 024 б  | 5      |
| 8   | `src/utils`                                                                                  | 35 027 б  | 4      |
| 9   | `src/features/adblock`                                                                       | 32 430 б  | 5      |
| 10  | `src/features/search`                                                                        | 21 682 б  | 2      |
| 11  | `src/features/translator`: `rules.ts`, `dom.ts`                                              | 18 008 б  | 2      |
| 12  | Хвосты: `src-tauri/src/updater.rs`, `src/vite-env.d.ts`                                      | 8985 б    | 2      |

Всего в очереди 649 830 б в 69 файлах. Прогноз экономии: −30% верно для `.ts`
и `.rs`, для `.vue` и `.scss` — от 1 до 30% в зависимости от шапки.

Каталогами чистить дешевле: в `src/api` девять файлов с однотипными
комментариями к запросам, одна проверка на всю пачку.

## Запись через API: что выяснилось

Порог касается текста, который отправляется, а не того, что лежит в репозитории.
Почищенный файл легче исходного, поэтому чистка тяжёлого файла через API
безопаснее, чем кажется.

Оценка порога в 25 КБ была заниженной. `translator/index.ts` записан целиком
одним вызовом на 34 677 б, перечитка подтвердила целостность. Значит, обрыв
`proxy.rs` был не чистым эффектом размера. Правило: размер сам по себе не
запрет, но контрольная перечитка после каждой записи обязательна: обрыв API
отдаёт как успех.

Партия 8 дала второй довод за перечитку, уже не про обрывы: трижды подряд в
текст попадал мусор генерации — оборванное слово в `SETTINGS-UI.md` (`35eaf91`),
посторонний иероглиф внутри комментария `SettingsProxyCard.vue` (`f70d026`)
и семь чужих букв вместо «ё» в этом журнале. Все три дефекта прошли бы
`typecheck` и сборку незамеченными: это текст внутри комментария или документа.
Ловит их только чтение глазами после записи.

Правки на несколько строк в тяжёлом файле через API не гоняем: партия 7 — восемь
строк в файле на 46 КБ, дешевле и безопаснее поправить локально. Исключение —
`LoggerModal.vue` в партии 8: две правки в 16 КБ отправлены целиком ради того,
чтобы не дёргать человека среди партии.

Файл, созданный через API, проходит `prettier --check` не всегда, но чаще всего
проходит. Гонять `--write` вслепую не надо: достаточно `--check` в общем прогоне
партии и `--write` точечно, если ругнётся. `npm run format` бьёт по корневым
`*.md` и уже ломал `README.md` (`b2df266`, откачено `a8f1451`).

Записи в одну ветку идут только последовательно: четыре параллельные дали 409.

## Долги по задаче

- Пересобрать очередь по доле комментариев в файле вместо веса файла.
- Решить, переводить ли комментарии в `style.scss` с `/* */` на `//`: вторые не
  доживают до компиляции и снимут ~1 КБ с `dist/animori.css`. Меняет вывод
  сборки, поэтому отдельным решением и отдельным коммитом.
- Дописать в `docs/CODE-STYLE.md` абзац про SCSS: разделители секций разрешены,
  шапка под общим лимитом, `/* */` попадают в CSS, `//` нет.
- `docs/PROJECT-MAP-MODULES.md`: добавить ссылки на `docs/2.0.1/TRANSLATOR.md`
  и `docs/2.0.1/SETTINGS-UI.md`.
- `docs/PROJECT-MAP.md`: добавить ссылку на этот журнал.
