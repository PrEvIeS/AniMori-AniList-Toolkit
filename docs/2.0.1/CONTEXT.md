# Контекст текущего шага

Шаг: чистка комментариев, партия 11 — каталог `api/`.

Сделано: партия 10 закрыта (`c25347d`). Каталог `features/ui/` разобран
целиком: четырнадцать файлов почищены, `reload.ts` проверен и оказался
чистым. Код не менялся ни в одном коммите, правки только в комментариях.

Попутно сняты пять ссылок на удалённый `SETTINGS-UI.md`: причины теперь
сказаны на месте. Найдены они глазами: поиск по коду на GitHub ответил
«ничего не найдено», ибо его индекс отстаёт от свежих коммитов.
Доказательством отсутствия ссылки служит только чтение файла.

Второй вывод: разделители вида `// ==== Секция ====` в TS снимаются всегда,
а если разделитель нёс правило, оно остаётся обычным однострочным
комментарием у самого блока.

Следующее: партия 11 по `docs/CODE-STYLE.md`. Начать с просмотра всего
каталога `api/` и порядка по доле комментариев, брать пачками мелких
файлов, по коммиту на файл. Затем `core/`, дальше по списку в плане.

На руках у владельца: `git pull` → правка → `prettier --write` → `typecheck`
→ коммит → `push`.
