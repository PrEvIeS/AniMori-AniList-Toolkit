# Контекст текущего шага

Шаг: чистка комментариев, партия 15 закрыта.

Сделано: почищены `src/utils/` (`e5bc42c`, фикс символов `ea42647`,
`0d9b156`, `4058d8b`, `e197deb`), `src/features/adblock/` (`a82bb0d`,
`705aa45`, `13c0911`, `7eba63e`) и `src/features/search/` (`c59becc`,
`90993bf`). `adblock/impl.noop.ts` оставлен как есть. Код не менялся.

Главное об этих каталогах: лишним было всё, что привязано ко времени —
номера пунктов и этапов, строки монолита, датированные слепки DOM,
пересказы отменённых решений и обещания переписать файл на Vue.
Шапка `adblock/index.ts` уменьшилась вдвое без потери смысла.

Сохраняли только невыводимое из кода: разделение труда CSS- и сетевого
блокировщиков, запрет опираться на `data-v-*`, риски №3 и №4, почему
разведчик не попадает в юзерскрипт и молчит до горячей клавиши,
404 у корневых полей Character/Staff и игнор `&limit` у `/search` Shikimori.

Следующее: партия 16 — хвосты `src/features/translator/`, потом проверить
`src/features/exporter/sync-api.ts`.

Правило работы: запись одного файла за раз и обязательное перечитывание
после неё: в `e5bc42c` вместо «всё» ушёл битый символ, правка — `ea42647`.

Проверка на машине владельца: `git pull` → `typecheck`, сборка один раз
в конце партии.
