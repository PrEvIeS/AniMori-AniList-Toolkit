# Контекст текущего шага

Шаг: чистка комментариев, партия 14 — `src/features/scanner/` и `src/features/exporter/`.

Сделано: партия 13 закрыта (`d9a4495`). Семь файлов `src/features/media/`
почищены за три итерации, по коммиту на файл. Код не менялся нигде.

Главное об этом каталоге: шапки писались в несколько заходов — по абзацу
на итерацию, до двадцати четырёх строк в `player.ts`. Ценным оказалось
то, как виджет уживается с чужими каркасами: React пересобирает сайдбар,
поэтому mount() зовут многократно, а кнопка плеера живёт в панели Vue.

Вывод партии: в виджетах сохранять надо условия молчания и повторного
входа — отметки вроде `settledAniId`, без которых каждая мутация страницы
стоит запроса. Описание разметки читается из самого кода.

Следующее: партия 14 по `docs/CODE-STYLE.md`. Начать с просмотра обоих
каталогов целиком, брать пачками мелких файлов, по коммиту на файл.
Дальше по списку в плане.

Правило работы: запись одного файла за раз и обязательное перечитывание
после неё. При перезаписи целиком следить не только за текстом, но и за
пунктуацией кода: потерянная висячая запятая сломала бы `prettier --check`.

На руках у владельца: `git pull` → правка → `prettier --write` → `typecheck`
→ коммит → `push`.
