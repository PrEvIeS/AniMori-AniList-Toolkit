# Контекст текущего шага

Шаг: чистка комментариев, партия 12 — каталог `core/`.

Сделано: партия 11 закрыта (`2a9818a`). Девять файлов `api/` почищены
за три итерации, по коммиту на файл. Код не менялся нигде.

Главное об этом каталоге: его шапки были летописью переездов на мост,
до шестидесяти строк. Ценными в них оказались только запреты и цифры
замеров: они переехали к самим объявлениям, остальное снято.

Вывод партии: однострочники вида «см. пункт 3 в шапке» надо делать
самодостаточными в том же коммите, где сносится шапка, иначе ссылка
повисает в пустоте. Так было с запретом куков в `api/anilist.ts`.

Следующее: партия 12 по `docs/CODE-STYLE.md`. Начать с просмотра всего
каталога `core/` и порядка по доле комментариев, брать пачками мелких
файлов, по коммиту на файл. Особое внимание `net-health.ts`, `proxy.ts`
и `db.ts`: на них ссылаются комментарии из `api/`. Дальше по списку в плане.

Правило работы: запись одного файла за раз и обязательное перечитывание
после неё: так в этой партии нашлись две свои опечатки, исправленные
отдельными коммитами (`343a9b0`, `e74a247`).

На руках у владельца: `git pull` → правка → `prettier --write` → `typecheck`
→ коммит → `push`.
