# Контекст текущего шага

Шаг: чистка комментариев, партия 13 — каталог `media/`.

Сделано: партия 12 закрыта (`a00ad57`). Десять файлов `core/` почищены
за четыре итерации, по коммиту на файл. Код не менялся нигде.

Главное об этом каталоге: шапки были привязаны к монолиту и нумерации
плана 2.0.0 — сняты везде. Ценными оказались не описания механики,
а запреты: дефекты A3 и A5, запрет на имена хостов в `net-health.ts`,
запрет на литерал у старого ключа `set_titles`.

Вывод партии: в ядре сохранять надо то, что ломается тихо — без ошибки
в журнале и без падения тайпчека. Описание того, что делает функция,
читается из самой функции.

Следующее: партия 13 по `docs/CODE-STYLE.md`. Начать с просмотра всего
каталога `media/`, брать пачками мелких файлов, по коммиту на файл.
Дальше по списку в плане.

Правило работы: запись одного файла за раз и обязательное перечитывание
после неё. Целостность проверять по содержимому, а не по ответу сервера:
`db.ts` на 16,6 КБ дошёл целиком, но порог обрыва близко.

На руках у владельца: `git pull` → правка → `prettier --write` → `typecheck`
→ коммит → `push`.
