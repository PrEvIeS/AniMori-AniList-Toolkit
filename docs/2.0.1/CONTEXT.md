# Контекст текущего шага

Шаг: реформа документации по `docs/DOC-RULES.md`.

Сделано: 2.0.0 сведён к `PLAN.md` и `CONTEXT.md` (`054eaba`); для 2.0.1 собран
`PLAN.md` (`17e28ea`), решения этапа 5 перенесены строками в `docs/DECISIONS.md`
(`4ba29f4`).

Следующее: удалить 13 старых документов `docs/2.0.1/`, переадресовать шапки
пяти файлов `features/ui/` с `SETTINGS-UI.md` на реестр, затем почистить три
карты и вернуться к партии 10 чистки комментариев.

На руках у владельца: прогон `git pull` → `git rm` → `prettier --write` → `typecheck`
→ коммит → `push`.
