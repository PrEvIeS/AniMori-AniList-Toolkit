# Контекст текущего шага

Шаг: чистка комментариев, партия 10 — остаток `features/ui/`.

Сделано: реформа документации закрыта полностью. Журналы 2.0.1 удалены,
шапки семи файлов кода переведены на реестр (`d758528`); три карты
обновлены (`6c102e7`, `6b8016f`, `fb3ed84`); галочки реформы в плане
проставлены (`cdb8e85`); в `CODE-STYLE.md` битая ссылка заменена на план
и дописан раздел про SCSS (`a8508ad`).

Проверено по дереву ветки: в `docs/2.0.1/` ровно два файла, ссылок на
удалённые версионные документы ни в коде, ни в `docs/` не осталось.

Следующее: партия 10 по `docs/CODE-STYLE.md`, порядок — по доле комментариев.
Первый кандидат `features/ui/settings.ts`: шапка на 11 строк с пустыми `//`,
номер пункта плана и пересказ истории «Было — Стало», то есть запреты 1 и 3
сразу. Дальше по очереди остальные файлы каталога, затем `api/` и `core/`.

На руках у владельца: `git pull` → правка → `prettier --write` → `typecheck`
→ коммит → `push`.
