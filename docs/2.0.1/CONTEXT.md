# Контекст текущего шага

Шаг: чистка комментариев, партия 15 — `src/utils/`, `src/features/adblock/`,
`src/features/search/`.

Сделано: партия 14 закрыта (`5f9c712`). Шесть файлов `scanner/` и
`exporter/` почищены за три итерации, по коммиту на файл: `f17eff7`,
`4449828`, `f329893`, `718fb56`, `9bf3923`, `049afff`, `9fba049`, `5f9c712`.
Код не менялся нигде.

Главное об этих каталогах: шапки были пересказом монолита и списком
пунктов этапов — до тридцати трёх строк в `sync-state.ts`. Ценными
оказались только ловушки: id корня `am-sync-overlay` против мёртвого блока
`!important` в `style.scss`, неудаляемый `style="display: flex"` у `.amk-overlay`,
цикл импортов вокруг `compare.ts` и кэш токена в `api/anilist.ts`.

Вывод партии: в модалках сохранять надо только то, что невидно из кода:
столкновения со стилями, различия десктопа и браузера, причины разбиения
на файлы. Разметка и порядок шагов читаются из самого кода.

Следующее: партия 15 по `docs/CODE-STYLE.md`. Начать с `src/utils/`, брать
пачками мелких файлов, по коммиту на файл.

Правило работы: запись одного файла за раз и обязательное перечитывание
после неё: в `718fb56` вместо кавычки ушёл знак `±`, правка — `e704d1c`.

Проверка на машине владельца: `git pull` → `typecheck`, сборка один раз
в конце партии.
