# Контекст текущего шага

Шаг: чистка комментариев закончена, партия 16 закрыта.

Сделано: `translator/dom.ts` (`5768257`), `translator/rules.ts` (`44b4517`),
`exporter/sync-api.ts` (`1b2c4cd`). `translator/index.ts` уже укладывался
в лимиты и не правился. Код не менялся.

Лишним было то же, что и раньше: номера этапов и пунктов, строки
монолита 1.9.1, пересказ РИСКОВ из `DECISIONS.md`, история «было —
стало» в доках функций. Шапка `sync-api.ts` была на 20 строк.

Сохранили невыводимое из кода: в переводчике — запрет менять порядок
правил (они пересекаются, решает первое совпадение) и примеры строк
AniList у регэкспов; в экспортере — анонимность запроса в оболочке,
повтор по 429 вместо молчаливого обрыва списка и запрос своего списка
с токеном — без него весь список перезаливается заново.

Проверка на машине владельца: `typecheck` после каждой итерации зелёный,
`build` и `build:tauri` в конце партии прошли без ошибок.

Следующее: пункт 5.3.8 — авторизация прокси для страницы
(`BasicAuthenticationRequested`, только Windows), затем 5.3.9 и 5.4.

Правило работы: запись одного файла за раз и обязательное перечитывание
после неё: в `e5bc42c` вместо «всё» ушёл битый символ, правка — `ea42647`.
Файл в 21 КБ (`sync-api.ts`) записался целиком без обрыва.
