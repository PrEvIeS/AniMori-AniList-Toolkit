# Текущий шаг

Раздел 1 закрыт и проверен: тайпчек, две сборки и cargo check чисты.
За владельцем осталось переименование страницы на Greasy Fork.

Раздел 2 закрыт целиком: 2.4 сделан в `2fe4181`, три оставшихся
пункта сняты отказом, причины — в `DECISIONS.md` этой же папки.

Раздел 4 закрыт кроме окна без рамки: адрес страницы и полный экран
проверены владельцем на живом окне, обе сборки и cargo check чисты.

Вопрос 3.2 закрыт 12 августа: остаёмся на `edition = "2021"`, правок
в этой версии нет. План на день перехода — в `DECISIONS.md`.

Следующий шаг: остался один вопрос владельцу — 3.1 про 403 приватных
данных Shikimori в пробах; после него версия уходит в выпуск: номер,
`CHANGELOG.md`, слияние и тег.

Что учесть:

- Номер версии поднимается в двух местах: `package.json` и `src-tauri/Cargo.toml`.
- Новая команда оболочки — четыре места: `lib.rs`, `build.rs`, `capabilities/default.json` и контракт `IBridge.ts` с обеими реализациями.
- Общее состояние блока навигации живёт в `nav-state.ts`: импорт из `NavPanel.vue` в `nav.ts` замкнул бы граф.
- `src/style.scss` — 46 КБ, через API целиком не пройдёт: резать на части либо отдать владельцу.
- Резка style.scss по номерам строк — только с проверкой баланса скобок после: перевᑑрнутый диапазон в PowerShell возвращает строки в обратном порядке, а не пустоту.
- cargo check требует сначала `npm run build:tauri`: `lib.rs` вклеивает `dist/animori.tauri.*`.
- Формат проверять до коммита: Prettier сам сжимает импорты и атрибуты до ста символов, ручной перенос он считает ошибкой.
- `SettingsProxyCard.vue` не проходит Prettier и без наших правок — старый долг, чинить отдельным коммитом.
- Наличие ключа в собранном бандле ничего не доказывает: алиасы `@*-impl` подменяют реализацию, смотреть надо в код, а не grep по строке.
- Смена дефолтов дойдёт до людей только выпуском — раздел 5 плана.
- Сохранённый выбор тех, кто тумблер включал, остался как был.
