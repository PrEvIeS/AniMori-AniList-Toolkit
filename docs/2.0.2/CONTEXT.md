# Текущий шаг

Раздел 1 закрыт и проверен: тайпчек, две сборки и cargo check чисты.
За владельцем осталось переименование страницы на Greasy Fork.

Из раздела 2 закрыт пункт 2.4 (`2fe4181`): шесть правил экспортера
сняты, селектора в коде больше нет, prettier и сборка юзерскрипта чисты.

Следующий шаг: остаток раздела 2 — пункты 2.1, 2.2 и 2.3. Порядок
за владельцем: 2.3 самый дешёвый по смыслу, но снова упирается в style.scss.

Что учесть:

- `src/style.scss` — 46 КБ, через API целиком не пройдёт: резать на части либо отдать владельцу.
- Резка style.scss по номерам строк — только с проверкой баланса скобок после: перевёрнутый диапазон в PowerShell возвращает строки в обратном порядке, а не пустоту.
- cargo check требует сначала `npm run build:tauri`: `lib.rs` вклеивает `dist/animori.tauri.*`.
- Смена дефолтов дойдёт до людей только выпуском — раздел 4 плана.
- Сохранённый выбор тех, кто тумблер включал, остался как был.
