# Журнал разработки — Этап 2: UI на Vue 3

> Ветка: `desktop-dev-2`
> Отвётвлена от: `desktop-dev-1`, коммит `0927dd6` (закрытый Этап 1, версия 2.0.0)
> Старт: 30 июля 2026 года
> Состояние: итерация 3 (пункт 2.3) закрыта подтверждением разработчика; работа приостановлена перед пунктом 2.2
> План этапа: `docs/refactoring/RM2.md`. Риски: `docs/refactoring/AUDITION.md`
> Стабильная версия и точка отката: ветка `desktop-dev-1`

## 1. Цель и границы этапа

Цель Этапа 2 — заменить генерацию интерфейса строками и ручное управление DOM
компонентами Vue 3 (Composition API). Bridge и Tauri относятся к следующим этапам.
Бизнес-логика ядра не переписывается: меняется только слой представления.

Критерий приёмки: императивный UI заменён Vue-компонентами, поведение совпадает
с версией 2.0.0 из `desktop-dev-1`, а сборка юзерскрипта остаётся единственным таргетом.

История Этапа 1 осталась в `desktop-dev-1`. Этот журнал содержит только Этап 2.

## 2. Порядок работ

Принятый порядок: 2.1 → 2.5 → 2.3 → 2.2 → 2.4 → 2.6 → 2.7 и 2.8 → 2.9.

Причины отклонения от нумерации RM2:

1. Логгер идёт раньше настроек: он меньше и уже развязан с ядром через
   `registerLogSink()`, поэтому на нём дешевле проверить монтирование и реактивный поток.
2. Реестр приложений сделан в 2.1, а не в 2.9: защита от zombie-компонентов нужна
   с момента появления первого Vue-корня.
3. Медиа-виджеты на этом этапе не переводятся на Vue: они инжектируются внутрь
   React-дерева AniList, в отличие от безопасных модалок в `document.body`.
4. Фиксированный `emit` для динамического реестра кнопок панели не вводился в 2.5:
   связывание с модалками будет завершено в 2.6.

## 3. Актуальные риски

- **РИСК №3 — React против Vue.** Все приложения регистрируются в `vue-mounter`;
  доступны `unmountApp()` и `unmountAll()`. SPA-связывание остаётся на 2.9.
- **РИСК №4 — рекурсия мутаций.** `vue-mounter` добавляет `am-notr` до вставки корня
  в документ и до `mount()`. Переводчик не должен заходить в Vue-разметку.
- **РИСК №6 — память логгера.** Закрыт в итерации 3 кольцевым UI-буфером на 500 записей.
- **Вес бандла.** `build.minify: false` оставляет рантайм Vue неминифицированным.
  Решение по минификации отложено до приёмки этапа.

## 4. Состояние этапа

### 2.1. Среда и стратегия монтирования — ЗАВЕРШЕНО

- [x] `vue`, `@vitejs/plugin-vue` и `vue-tsc` подключены
- [x] `src/utils/vue-mounter.ts`: изолированные корни и реестр приложений
- [x] `am-notr` добавляется до монтирования
- [x] повторное монтирование не плодит дубликаты
- [x] доступен `unmountAll()` для будущего менеджера жизненного цикла
- [x] SFC входят в строгую типопроверку
- [x] локальная сборка и браузерная проверка пройдены

### 2.2. Настройки — `SettingsModal.vue` — СЛЕДУЮЩАЯ ИТЕРАЦИЯ, НЕ НАЧАТА

- [ ] восемь вкладок на локальном состоянии
- [ ] переключатели и поля доменов через `v-model`
- [ ] сохранение через `core/settings.ts`
- [ ] выбор источников названий, акценты и пользовательские ссылки
- [ ] редактор словаря: поиск, импорт, экспорт и счётчик
- [ ] ссылка авторизации AniList собирается конкатенацией
- [ ] императивное содержимое `features/ui/settings.ts` удалено

### 2.3. Логгер — `LoggerModal.vue` — ЗАВЕРШЕНО

- [x] записи рендерятся через `v-for`
- [x] семь фильтров и поиск работают через `computed`
- [x] группировка API/DB/QUEUE сохранена
- [x] JSON-вьюер и stack trace раскрываются реактивно
- [x] счётчик новых записей и прокрутка вниз работают
- [x] дамп состояния включает `listMountedApps()`
- [x] копирование, скачивание и очистка работают
- [x] кольцевой UI-буфер ограничен 500 записями — РИСК №6 закрыт
- [x] корень получает `am-notr` от `vue-mounter`
- [x] `logger-ui.ts` сведён к подписке, монтированию и регистрации кнопки
- [x] `vue-tsc --noEmit` чист, сборка успешна, браузерные тесты без аномалий

### 2.4. Сканер — `ScannerModal.vue` и `ScannerDiffCategory.vue`

- [ ] главное окно и сворачиваемые категории расхождений
- [ ] реактивные статусы сканирования
- [ ] ignore-лист на реактивном `Set`
- [ ] сравнение избранного и поиск связанных сезонов сохранены
- [ ] бизнес-логика сверки не изменена

### 2.5. Панель действий — `ActionPanel.vue` — ЗАВЕРШЕНО

- [x] плавающие кнопки переведены в компонент
- [x] порядок задаётся `ACTION_ORDER`
- [x] кнопка плеера живёт в той же панели и скрывается при уходе со страницы
- [x] `actions.ts` сведён к состоянию, монтированию и публичному API
- [x] локальная сборка и браузерная проверка пройдены
- [ ] окончательное связывание панели и модалок — пункт 2.6

### 2.6. Связывание в `main.ts`

- [ ] заменить отдельные императивные инициализаторы монтированием Vue
- [ ] связать панель с публичным API модалок
- [ ] сохранить порядок `bootstrap()`: настройки загружаются до UI

### 2.7. Синхронизация — `SyncModal.vue`

- [ ] единый компонент экспорта и импорта
- [ ] предупреждение о публичности профиля Shikimori
- [ ] старая модалка удалена, логика переноса не изменена

### 2.8. Управление попапами плеера

- [ ] настройка блокировки всплывающих окон
- [ ] новый ключ в `AniMoriSettings`, по умолчанию выключен
- [ ] в юзерскрипте настройка пока не имеет потребителя

### 2.9. Менеджер жизненного цикла

- [ ] `unmountAll()` при смене роута
- [x] реестр приложений и детектор исчезновения корня готовы
- [ ] переводчик игнорирует всё внутри Vue-корней
- [ ] нет утечек и фантомных виджетов при SPA-переходах

### Приёмка этапа

- [ ] весь запланированный императивный UI заменён
- [ ] `vue-tsc --noEmit` и `vite build` чистые
- [ ] зафиксирован итоговый размер бандла
- [ ] пройдены проверки настроек, логгера, сканера и синхронизации
- [ ] пройдены SPA-переходы и Назад/Вперёд с открытыми модалками
- [ ] переводчик не обрабатывает Vue-разметку
- [ ] принято решение по минификации
- [ ] разработчик подтвердил закрытие этапа

## 5. Итерации и замеры

| Итерация | HEAD кода | Содержание | Модули | Размер |
| --- | --- | --- | --- | --- |
| база | `0927dd6` | результат Этапа 1 | 35 | 267.83 kB |
| 1 (2.1) | `665d53c` | Vue-конвейер и `vue-mounter` | 35 | 269.39 kB, gzip 69.58 kB |
| 2 (2.5) | `a80bbc0` | `ActionPanel.vue`, первый боевой SFC | 44 | 496.89 kB, gzip 122.20 kB, файл 504 813 байт |
| 3 (2.3) | `5a54e41` | `LoggerModal.vue`, буфер и реактивный поток | 47 | 518.65 kB, gzip 126.98 kB, файл 526 614 байт |

### Итерация 1 — среда и монтирование

Файлы: `package.json`, `vite.config.ts`, `tsconfig.json`, новый
`src/utils/vue-mounter.ts`. Коммиты кода: `5329436`, `20c09d3`, `db7dc16`, `665d53c`.

Ключевые решения:

- `vue` находится в `dependencies`, инструменты компиляции — в `devDependencies`;
- `vue()` выполняется до `monkey()`;
- и typecheck, и build используют `vue-tsc`;
- Options API, devtools и hydration-details выключены;
- корень получает `am-notr` до вставки и монтирования;
- `app.config.errorHandler` направляет сбои компонентов в общий `Logger`;
- `vue-mounter` не зависит от `GM_*` и готов к переносу на следующем этапе.

Проверка: typecheck чист, сборка успешна, в браузере аномалий нет. В этой итерации
Vue ещё вырезался tree-shaking, потому что боевого SFC не было.

### Итерация 2 — панель действий

Файлы: новые `action-panel-state.ts` и `ActionPanel.vue`; переписаны `actions.ts`,
`media/player.ts` и `media/index.ts`. Коммиты кода: `176d804`, `52f771b`, `3e79fed`,
`5dc4315`, `a80bbc0`.

Ключевые решения:

- компонент сам рендерит `#animori-actions`, чтобы сохранить существующую flex-разметку;
- состояние вынесено отдельно для исключения циклических импортов;
- кнопка плеера переведена с ручного DOM на реактивное состояние;
- динамический реестр кнопок сохранён, поскольку независимые модули регистрируют кнопки;
- сортировка выполняется в `computed`, а не зависит от порядка регистрации;
- `watchContainer: false`, так как панель монтируется непосредственно в `body`.

Проверка: typecheck чист, 44 модуля, 496.89 kB, gzip 122.20 kB, браузер без аномалий.
Рост сырого размера связан с `minify: false`; gzip вырос на 52.6 kB относительно
итерации 1. Решение по минификации отложено.

### Итерация 3 — логгер

Файлы: новые `logger-state.ts` и `LoggerModal.vue`; `logger-ui.ts` сведён к точке
монтирования. Коммиты кода: `c29a28a`, `75708c1`, `5a54e41`.

Ключевые решения:

1. Состояние вынесено в `logger-state.ts`, чтобы исключить цикл между точкой
   монтирования и компонентом.
2. `shallowRef<LogEntry[]>` ограничен последними 500 записями. Новые записи приходят
   через sink, при открытии выполняется синхронизация с `scriptLogs`.
3. Фильтрация и группировка выполняются в `computed displayItems`. Группировка активна
   только при фильтре `ALL` и пустом поиске, как в прежней реализации.
4. Открытые группы, details и stack хранятся в `Set`-рефах и сбрасываются только при
   смене фильтра или поиска.
5. `v-html` используется только для JSON-вьюера; строки и ключи проходят `escapeHTML`.
6. Оверлей монтируется один раз, видимость управляется `v-if`. Корень получает `am-notr`.
7. Дамп состояния дополнен списком смонтированных Vue-приложений.
8. Императивные `createSingleLogEl`, `appendLogEntry` и `renderAllLogs` удалены.

Подтверждение разработчика от 30 июля 2026 года:

- `git pull --ff-only` выполнен без расхождения ветвей;
- `npm run typecheck` завершён без ошибок;
- `npm run build`: 47 модулей, 518.65 kB, gzip 126.98 kB, 1.14 с;
- размер `dist/animori.user.js`: 526 614 байт;
- ручные браузерные тесты пройдены без аномалий.

Относительно итерации 2 логгер добавил три модуля, 21.76 kB сырого и 4.78 kB gzip.
Итерация 3 закрыта. Следующая точка — пункт 2.2 (`SettingsModal.vue`), но работа над ним
пока не начиналась по прямому указанию разработчика.

## 6. Правила работы

- Файлы отправлять по одному и последовательно: параллельная запись в одну ветку даёт 409.
- Перед локальными коммитами выполнять `git pull --ff-only origin desktop-dev-2`.
- После изменений проверять `npm run typecheck`, `npm run build` и браузерное поведение.
- Журнал обновлять только после завершённой и подтверждённой итерации.
- Не запускать `npm audit fix --force`: несовместимое обновление может сломать
  `vite-plugin-monkey`.
- Установка зависимостей и сборка выполняются локально разработчиком.
- Решение по `build.minify` принять на приёмке этапа, а не менять во время миграции.

## 7. Точка остановки

**Остановились после подтверждённого завершения пункта 2.3.**

Текущий проверенный код: `5a54e41`; журнал закрытия итерации следует после него.
Проверенная сборка: 47 модулей, 518.65 kB, gzip 126.98 kB, файл 526 614 байт.
Следующий запланированный пункт: **2.2 — `SettingsModal.vue`**. Он ещё не начат.
