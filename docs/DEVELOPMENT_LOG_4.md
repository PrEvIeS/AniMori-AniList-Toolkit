# Журнал разработки AniMori — часть 4

## 0. Зачем новая часть

Часть 1 — `docs/DEVELOPMENT_LOG.md` (72 КБ, заморожена на `6327509`).
Часть 2 — `docs/DEVELOPMENT_LOG_2.md` (около 52 КБ, разделы 0–12).
Часть 3 — `docs/DEVELOPMENT_LOG_3.md` (53 КБ, разделы 0–7), заморожена здесь.

Порог заведения новой части снижен с 60–70 КБ до 45–50 КБ. Причина записана
в части 3, пункт 7.3, и она не теоретическая: при записи раздела 7 запись
оборвалась на середине и урезала файл с 38 до 23 КБ. Восстановление удалось
только потому, что полный текст ещё лежал в переписке. Запись в репозиторий
идёт полной перезаписью файла, поэтому чем крупнее журнал, тем дороже
каждая новая строка в нём. Часть 3 уже перевалила за новый порог, так что
эта итерация пишется с чистого листа.

Правило прежнее и оно здесь работает в полную силу: **старые части задним
числом не правятся**. Раздел 1 ниже опровергает гипотезу из пункта 7.4
части 3, а раздел 2 опровергает гипотезу из первой редакции этого же файла.
Оба исходных текста остаются на месте. Историю ошибок не переписывают, иначе
теряется главное — как именно рассуждение свернуло не туда.

---

## 1. Дефект A11: гипотеза была неверной

### 1.1 Что утверждалось в части 3

Пункт 7.4 части 3 назвал ведущей гипотезой глобальную проверку лимитов
в `processTransQueue()`:

```ts
if (isAniListRateLimited() || isShikimoriRateLimited() || isAnime365RateLimited())
```

Логика была такая: anime365 участвует только в резолве названий, но его
бэкофф останавливает весь цикл, включая перевод персонажей, которым anime365
не нужен вовсе. Звучало правдоподобно и хорошо ложилось на описание симптома
(«запросы к anime365 закончились, к Шикимори не начались»).

**Гипотеза опровергнута.** Она была построена на чтении кода без единого
наблюдения. В логе за 02.08 13:21 нет ни одного сообщения «Активен лимит API»
— ветка с проверкой лимитов не срабатывала ни разу за всю сессию.

Запись оставлена здесь как урок: правдоподобие механизма не является
доказательством того, что механизм сработал. Прежде чем называть версию
ведущей, нужен след в журнале приложения. Если следа нет — версия остаётся
версией, и говорить о ней надо именно так.

Сама по себе проверка лимитов действительно грубовата: остановка всей очереди
из-за источника, который нужен только одной из трёх веток, однажды выстрелит.
Но это спящий недостаток, а не причина A11. Отдельным дефектом не заводится,
пока не будет наблюдения.

### 1.2 Что показал лог

Лог сессии: 3912 строк, 231 промах кэша по тайтлам (MED2), 6 по персонажам
(CHR2) и 14 по авторам (STF3). Кэш IndexedDB был сброшен вручную
непосредственно перед проверкой, то есть условия наихудшие из возможных.

Хронология:

| Время | Событие | MED2 в ожидании | Персоны |
| --- | --- | --- | --- |
| 13:21:04 | Открыт список аниме, сплошные промахи кэша | растёт | — |
| 13:21:10.152 | Пачка №1: AniList за 294 мс, дальше anime365 по одному | 171 | — |
| 13:21:18.315 | Переход на страницу тайтла, поставлены 6 CHR2 и 14 STF3 | ~190 | 20 ждут |
| 13:21:22.775 | Пачка №4 — снова MED2, персоны хода не получили | 210 | 20 ждут |
| 13:21:32.765 | Таймаут зеркала smotret-anime.online на malId=31704 | в работе | 20 ждут |
| 13:21:37.775 | Таймаут anime365.ru, ERROR «Все зеркала недоступны» | в работе | 20 ждут |
| 13:21:38.789 | Лог обрывается, очередь всё ещё грызёт MED2 | в работе | 20 ждут |

Главное наблюдение: **очередь не вставала**. Она непрерывно работала все
двадцать секунд после перехода — просто не над тем, что видел пользователь.

### 1.3 Настоящая причина, часть первая: голодание по приоритету

Выбор работы в `processTransQueue()` был сделан строгим приоритетом:

```ts
if (pending.MED2.size > 0) await processMediaBatch()
else if (pending.CHR2.size > 0) await processPersonBatch('CHR2')
else if (pending.STF3.size > 0) await processPersonBatch('STF3')
```

Пока в MED2 остаётся хотя бы один элемент, до персонажей управление
не доходит никогда. Это учебное голодание по приоритету, и на страницах
с десятком карточек оно незаметно: MED2 пустеет за секунду. Заметным оно
становится ровно там, где очередь тайтлов длинная — то есть после списка.

### 1.4 Настоящая причина, часть вторая: мёртвый груз

К моменту перехода в MED2 лежало около 190 карточек **покинутой страницы**.
Их узлы оторваны от документа, писать перевод некуда, но очередь ничего
об этом не знает: на смене роута она не чистилась вообще. Каждый такой
элемент честно отрабатывался с походом в сеть.

Цена по времени считается прямо из лога. Пачка — 40 тайтлов. AniList отвечает
на всю пачку разом за 290 мс. Дальше в `processMediaBatch()` идёт строго
последовательный цикл, и внутри каждой итерации `resolveTitle()` ходит
в anime365 **по одному тайтлу**, по 250–300 мс на штуку. Пачка целиком —
12–15 секунд, пачек нужно шесть, итого около полутора минут до того, как
персонажи получат первый шанс.

Это в хорошем случае. В логе есть плохой: на `malId=31704` первое зеркало
отвалилось по таймауту через 5 секунд, второе — ещё через 5. Один невезучий
тайтл остановил всю очередь на десять секунд.

### 1.5 Правка

Коммит `e7cf29d`, файл `src/features/translator/index.ts`, 48 строк добавлено,
2 удалено.

**Персоны идут первыми.** Порядок в цикле развёрнут: сначала CHR2, затем
STF3, и только потом MED2. Обоснование не в том, что персонажи важнее,
а в двух свойствах: персоны бывают только на той странице, где сейчас стоит
пользователь, и пачка у них вчетверо меньше (10 против 40). Задержка для
тайтлов от такой уступки пренебрежимая, а видимый эффект мгновенный.

**Смена роута выбрасывает оторванные узлы.** Добавлена `dropDetachedEntries()`,
вызывается из `resetTranslatorRetries()` — та уже висит на смене роута под
именем задачи `translator:retries`, так что `main.ts` трогать не пришлось.
Функция проходит по очереди, отфильтровывает записи с `el.isConnected === false`,
и если у ключа не осталось ни одного живого узла — убирает ключ из `queue`,
ид из `pending` и запись из `attempts`.

Уже отправленные пачки это не ломает: их идентификаторы вынуты из `pending`
до начала обработки, а результат в любом случае ляжет в кэш и пригодится
при возврате на список.

### 1.6 Результат

Сборка чистая, живая проверка владельца — правка возымела эффект, персонажи
начинают переводиться сразу после перехода, без дополнительной перезагрузки.

**A11 закрыт.**

---

## 2. Дефект A12: медлительность очереди тайтлов

### 2.1 Как дефект был сформулирован сначала — и почему неверно

Первая редакция этого раздела называла две причины: последовательный обход
строк пачки в `processMediaBatch()` и таймауты зеркал anime365. Лечением
предлагался ограниченный параллелизм порядка четырёх одновременных
`resolveTitle()` плюс предохранитель на серию таймаутов.

**Первая половина этой формулировки неверна.** Она снова написана по коду
одного файла, без чтения того, что лежит уровнем ниже — ровно та же ошибка,
что разобрана в пункте 1.1, повторённая в тот же день.

Чтение `src/api/rate-limit.ts` показало: параллелизм в `processMediaBatch()`
не дал бы почти ничего. Шлюз `acquireSlot()` выстраивает всех вызывающих
в одну цепочку промисов и выдаёт разрешения строго по одному, соблюдая
`minIntervalMs`. Сколько бы запросов ни было запущено одновременно,
отправляться они будут с тем же интервалом. Наблюдавшиеся 250–300 мс
на тайтл — это не задержка сети и не последовательность цикла, это **наш
собственный ограничитель темпа**, работающий ровно как задумано.

### 2.2 Где на самом деле потолок

Ограничитель настроен так:

```ts
export const API_MIN_INTERVAL_MS = 300
export const API_WINDOW_MS = 60000
export const API_MAX_PER_WINDOW = 60
```

Первые шестьдесят запросов уходят с интервалом 300 мс, а дальше вступает
окно: шестьдесят запросов на шестьдесят секунд означает устойчивый темп
**один запрос в секунду**. Для списка из 231 тайтла с пустым кэшем это
минимум около четырёх минут, и никакой параллелизм этого не меняет.

В логе за 02.08 виден только разгон: сессия длилась 34 секунды, до потолка
окна дело не дошло. То есть наблюдение и здесь ограничено, и это важно
помнить — цифра «четыре минуты» посчитана из констант, а не измерена.

Отдельно стоит отметить происхождение самих констант. В комментарии
к `anime365Limiter` написано «режим тот же, что у Shikimori». У Shikimori
потолок задокументирован (5 запросов в секунду, 90 в минуту), у anime365 —
нет. Мы взяли для него чужие цифры, ещё и с запасом вниз: 60 вместо 90.
Поднимать бюджет вслепую нельзя: `ANIME365_FAIL_LIMIT` при серии 403
отключает источник на всю сессию.

### 2.3 Решение: приоритет видимого

Коммит `5fd515d`, файл `src/features/translator/index.ts`, 75 строк
добавлено, 5 удалено.

Если потолок темпа снять нельзя, надо сделать так, чтобы он перестал быть
важен. Пачка набиралась так:

```ts
const ids = [...pending.MED2].slice(0, MEDIA_BATCH)
```

То есть в порядке попадания в очередь, а он задаётся порядком обхода DOM
в `debouncedFindContent()` — сверху вниз по всему документу. Человек,
открывший длинный список и пролиставший вниз, ждал, пока пройдут все
карточки выше. При темпе один запрос в секунду **длина очереди определяла
всё**.

Добавлены две функции. `entryDistance(key)` считает расстояние от элемента
до видимой области в пикселях: ноль — элемент на экране, оторванные
и неотрисованные узлы получают бесконечность и уходят в хвост. Ключ может
держать несколько элементов (карточка в списке и та же ссылка в боковом
блоке), поэтому берётся ближайший. `pickBatch(kind, size)` сортирует
ожидающие по этому расстоянию и берёт первые `size`, попутно снимая
их с `pending`.

Оба сборщика пачек, `processMediaBatch()` и `processPersonBatch()`, теперь
зовут `pickBatch` вместо `slice`. Порядок отбора меняется, состав очереди —
нет: ничего не выбрасывается, невидимое просто дозаполняется фоном и оседает
в кэше.

Механика повторов не задета: `requeue()` и `returnToQueue()` кладут
идентификатор обратно в `pending`, и следующий отбор посчитает расстояние
заново.

Единственная новая нагрузка — `getBoundingClientRect()` на каждый ожидающий
ключ перед пачкой. Вызов заставляет браузер синхронно пересчитать вёрстку,
поэтому существенно, что все чтения идут подряд, без записей в DOM между
ними: раскладка считается один раз. Для двух сотен элементов это доли
миллисекунды против секунд ожидания сети.

### 2.4 Что решено не трогать

**Таймауты зеркал.** Владелец возразил: за всё время эксплуатации anime365
в таймауты не упирался, единственный случай в логе (`malId=31704`) —
одиночный сбой сети, а не система. Возражение принято, `MIRROR_TIMEOUT_MS`
остаётся 5000, перебор зеркал не меняется. Если серия таймаутов когда-нибудь
станет наблюдаемой, вернёмся с логом на руках.

**Бюджет ограничителя.** Не трогаем до измерения. Осмысленный первый шаг,
если скорость снова станет проблемой, — писать в журнал время ожидания слота
отдельно от времени ответа сервера. Тогда будет видно, что именно тормозит,
и разговор пойдёт по цифрам, а не по догадкам.

### 2.5 Результат

Сборка чистая, живая проверка владельца — результат удовлетворительный.

**A12 закрыт** в части приоритета. Абсолютная скорость холодного прохода
по длинному списку не изменилась и измениться не могла: она упирается
в бюджет ограничителя. Изменилось то, что скорость перестала быть заметной.

---

## 3. Дефект A13: панель навигации на серверных страницах (не воспроизводится)

Владелец сообщил: плавающие кнопки «вперёд», «назад» и «обновить» появляются
не на всех внутренних страницах. На странице копирования токена AniList
панели не было, и выйти оттуда было нечем.

Через несколько минут проблема была снята самим владельцем: при повторной
проверке панель появилась. Расследование свёрнуто на середине, правок
не вносилось.

**Статус — «не воспроизводится», а не «решено».** Дефект, исчезнувший сам,
может вернуться, поэтому запись остаётся.

**Правдоподобный механизм, не доказанный.** Страница копирования токена —
не маршрут SPA, а обычная серверная страница с полной загрузкой документа.
В десктопной сборке бандл подаётся через `initialization_script`, который
выполняется **до создания DOM**. До правки A2 старт готовности документа
не ждал: при `document.body === null` монтирование молча возвращало `null`,
и панель не появлялась. До правки A1 любой упавший шаг старта уносил с собой
весь остаток, включая `initNavPanel()`. На переходах внутри SPA это
не всплывало — документ давно готов; на полной загрузке всплывало.

Против этой версии есть возражение по времени: A1 и A2 уехали в сборку
задолго до жалобы. Либо проверялась старая сборка, либо дефект плавающий.
Ни то ни другое не подтверждено.

**Остаточный риск, который стоит держать в голове отдельно от самого
дефекта.** Блок навигации — единственный выход из окна, и он зависит
от успешного прохождения старта. Горячие клавиши тоже вешает бандл:
`installHotkeys()` в `src/features/ui/nav.ts` (Alt со стрелками)
и `initReloadControls()` в `src/features/ui/reload.ts` (F5, Ctrl+R). Если
бандл не поднимется, пользователь заперт в окне и починить это изнутри
невозможно. Разумный ход на будущее — отвязать аварийный выход от бандла,
например отдельным маленьким скриптом без сборки прямо в `lib.rs`. Работа
не начата и в план пока не поставлена.

Если дефект повторится: выгрузить лог и посмотреть, дошёл ли старт до шага
«блок навигации». Список шагов `bootstrap()` пишется в журнал приложения
поимённо, так что ответ будет однозначным.

Непроверенная до конца побочная версия: CSP страницы могла заблокировать
инъекцию стилей из `css_injection_script()`. Тогда панель существует,
но без стилей уезжает в конец `body` и невидима. Проверяется осмотром
элемента `#animori-nav` в отладчике, если случай повторится.

---

## 4. Служебное

**Коммит `cf49292`.** В `.gitignore` добавлен `src-tauri/permissions/` —
каталог автоматических разрешений, который `build.rs` генерирует под команды
`animori_reload` и `animori_open_external`. Он пересоздаётся при каждой
сборке, до сих пор в репозитории не было, и локально с CI всё собиралось
без него. Заодно закоммичен `src-tauri/Cargo.lock` после сборки.

Оговорка: происхождение каталога определено по `build.rs` и составу
`capabilities/default.json`, а не по наблюдению за сборкой. Если после чистого
клона `npm run build:tauri` начнёт ругаться на неизвестные разрешения —
строку из `.gitignore` надо убрать и каталог версионировать. Проверка
бесплатная и всплывёт немедленно.

**Рабочий процесс.** Журнал пишется напрямую через API GitHub, код — локально.
При записи в журнал между пушами локальный `git push` отвергается
с «fetch first». Лечение всегда одно: `git pull --rebase`, затем `git push`.
Именно rebase, а не merge: конфликтов между `docs/` и `src/` не бывает,
а лишний узел слияния историю только засоряет. Договорённость: после каждой
записи в журнал сразу предупреждать о необходимости подтянуть изменения.
За 02.08 договорённость сработала: перед пушем `5fd515d` предупреждение было
выдано заранее, конфликта не возникло.

---

## 5. Состояние дефектов

| № | Дефект | Состояние |
| --- | --- | --- |
| A1 | `bootstrap()` без `catch` | Закрыт, `c063de0` + `24ff8f9` |
| A2 | Никто не ждёт готовности DOM | Закрыт, `24ff8f9` |
| A3 | `openDB()` не обрабатывает `blocked` | Закрыт, `c063de0` |
| A4 | Потеря перехода при `isLoading` | Закрыт, `b4aecb9` |
| A5 | Стартовый адрес объявлен обработанным | Закрыт, `26d2215` |
| A6 | Маркер `queued` ставится до `await` | Закрыт, `6ff7ead` |
| A7 | Потеря пробуждения очереди | Закрыт, `6ff7ead` |
| A8 | `attempts` не обнуляется | Закрыт, `6ff7ead` |
| A9 | Словарь: сеть без таймаута и без повтора | Открыт, этап C |
| A10 | Нет идемпотентного дожима и сторожа | Открыт, этап C |
| A11 | Персоны голодают за очередью тайтлов | Закрыт, `e7cf29d` |
| A12 | Пачка набиралась без учёта видимого | Закрыт, `5fd515d` |
| A13 | Нет панели навигации на серверных страницах | Не воспроизводится |
| A14 | Сетевая разведка перехватывала горячие клавиши в браузере | Закрыт, `c36e20b` |

---

## 6. Точка остановки

- Ветка `desktop-dev-4`, вершина `5fd515d`.
- Этапы A и B закрыты полностью, сверх плана закрыты A11 и A12.
- Обе проблемы, заявленные владельцем в начале итерации, разобраны: стаггер
  закрыт правками A1–A8, A11 и A12; вторая (панель навигации) снята самим
  владельцем и лежит как A13 со статусом «не воспроизводится».
- Ближайшее — **этап C**: параллельные `openDB()` и загрузка словаря, таймаут
  и один повтор для словаря (`registerRetranslateCallback` для этого уже есть),
  идемпотентный `ensureAll()` в первые пять секунд после старта и по возврату
  фокуса на окно. Это дефекты A9 и A10.
- Предрелизный блок остаётся отложенным: инсталлятор NSIS в автосборке
  (`.github/workflows/release.yml`, артефакты `animori.user.js`
  и `AniMori_Setup.exe`), затем разбор автообновлений с нуля, начиная
  с объяснения механизма, а не с кода.
- Замеры сборки после `6ff7ead`: 81 модуль, `animori.tauri.js` 561.63 КБ
  (gzip 134.33), `animori.tauri.css` 46.76 КБ (gzip 8.56), юзерскрипт
  622.61 КБ (gzip 147.59). После `e7cf29d` и `5fd515d` замеры не снимались:
  обе правки мелкие, заметного изменения веса не ожидается.
- Урок итерации, повторённый дважды за один день: **гипотеза, построенная
  на чтении одного файла, стоит недорого**. A11 разбился о журнал приложения,
  A12 — о чтение `rate-limit.ts` уровнем ниже. Прежде чем называть причину,
  надо дочитать до конца тот слой, где эта причина якобы живёт.

*Точка остановки обновлена в пункте 7.7 — этот раздел оставлен как есть.*

---

## 7. Адблок разведён по сборкам

### 7.1 Повод: сверка паритета перед релизом

Владелец счёл работоспособность приложения удовлетворительной и объявил
готовность к релизу. Перед этим он попросил проверить, не отстала ли
браузерная сборка (`npm run build`) от десктопной (`npm run build:tauri`)
по функционалу.

**Скрипт не отстал.** Точка входа одна, `src/main.ts`, и весь состав
возможностей у сборок общий. Платформенных развилок ровно пять, все сделаны
одинаково — ранним выходом по `Bridge.platform !== 'tauri'`:
`ui/nav.ts`, `ui/reload.ts`, `ui/links.ts`, `adblock/net-block.ts`
и `api/shikimori-user.ts`. Первые три закрывают то, что браузер умеет сам
(навигация, перезагрузка, внешние ссылки). Последняя — место, где скрипт
наоборот **впереди** десктопа: в браузере запрос к Шикимори идёт
с `credentials: 'include'` и подхватывает сессию пользователя.

Заодно записана ловушка сборки: `emptyOutDir: !isTauri`. Юзерскриптовая
сборка чистит `dist/`, тауриная — нет. В CI порядок команд имеет значение:
сначала `build`, потом `build:tauri`, иначе первый снесёт артефакты второго.

### 7.2 Что нашлось: разведчик без проверки платформы

Единственная настоящая находка сверки — `initNetProbe()` из
`src/features/adblock/net-probe.ts` вызывался без всякой проверки платформы.
В браузере это означало живой перехват Ctrl+Shift+S и Ctrl+Shift+A ради
инструмента, которым в браузере всё равно нельзя воспользоваться:
собранный им отчёт нужен для правки списков блокировки в Rust-слое.
Заведено как A14.

Здесь же — моя ошибка, поправленная владельцем. Я предложил выбросить
`net-probe` из обеих сборок, назвав его просрочённым. Возражение владельца:
модуль оставлен в десктопе намеренно, на случай обновления списков
блокировщика. Возражение принято целиком, формулировка была слишком
категоричной. Модуль остаётся в десктопе, из браузерной сборки уходит.

### 7.3 Решение: в юзерскрипте адблока нет вовсе

Блокировка рекламы у нас двухслойная: сетевой слой в Rust (`adblock.rs`,
списки в WebView) и DOM-слой в бандле (`features/adblock/index.ts`, снятие
контейнеров Venatus и Gamera). В браузере доступен только второй слой,
и он заведомо слабее любого расширения. Тумблер в настройках при этом обещал
полноценную блокировку — то есть обещал больше, чем делал.

Решение владельца: **в браузерной сборке блокировщика нет совсем**. Кому
нужна блокировка рекламы — ставит uBlock Origin в два клика и получает
результат лучше нашего. Отдельно отмечено: версия 2.0 с адблоком ни разу
не релизилась, действующих пользователей эта правка не задевает, оговорки
в changelog не требуется.

### 7.4 Механика: псевдопуть `@adblock-impl`

Выбран тот же приём, что уже работает для моста (`@bridge-impl`): развилка
решается на этапе сборки, а не проверкой в рантайме. Вырезанный код тогда
не попадает в бандл вообще, а не лежит в нём мёртвым грузом.

- **`4af49b2`** — заведены два файла реализации. `impl.desktop.ts`
  переэкспортирует настоящие `initAdblock`, `destroyAdblock`, `syncAdblock`,
  `getBlockedCount`, `initNetProbe`, `destroyNetProbe`. `impl.noop.ts` даёт
  шесть пустышек, `getBlockedCount` возвращает 0. Алиас `@adblock-impl`
  добавлен в `vite.config.ts` (между `@bridge-impl` и `@`) и в `paths`
  из `tsconfig.json` — для тайпчекера он всегда указывает на десктопную
  реализацию, сигнатуры у обеих одинаковые.
- **`c36e20b`** — `main.ts` берёт адблок и разведку только из `@adblock-impl`.
  Заголовок `net-probe.ts` переписан: это инструмент сопровождения десктопной
  сборки. Первой строкой `initNetProbe()` встал выход по платформе — страховка
  на случай прямого импорта в обход алиаса (A14 закрыт).
- **`c15ba6c`** — `settings-state.ts`: `syncAdblock` тоже из алиаса, добавлен
  флаг `isAdblockAvailable = Bridge.platform === 'tauri'`, сеттер `hideAds`
  пишет `set_block_popups` только когда флаг поднят.
- **`211a42b`** и **`5702e0a`** — строка настроек, см. 7.5.

Потребители `getBlockedCount` проверены отдельно: `logger-state.ts`,
`LoggerModal.vue` и `action-panel-state.ts` из `features/adblock` ничего
не тянут, так что заглушка их не задевает.

### 7.5 Строка настроек: две итерации

В `211a42b` я закрыл тумблер условием `v-if="isAdblockAvailable"`, но вместо
него в браузере оставил строку-подсказку про uBlock Origin. Обоснование было
такое: блокировку рекламы всё равно будут искать в настройках, и пустое место
ответа не даёт.

**Владелец не согласился, и он прав.** Неуправляемая строка в столбце
тумблеров читается как сломанный переключатель, а не как совет. В `5702e0a`
ветка `v-else` убрана: в браузере строки нет вообще. Карточка «Прочее»
остаётся с одним «Логгером» — не пустеет, вид не страдает.

### 7.6 Замеры

| Сборка | Было (`6ff7ead`) | Стало (`5702e0a`) |
| --- | --- | --- |
| Юзерскрипт | 622.61 КБ (gzip 147.59) | 616.60 КБ (gzip 145.75) |
| `animori.tauri.js` | 561.63 КБ (gzip 134.33) | 563.69 КБ (gzip 135.02) |
| `animori.tauri.css` | 46.76 КБ (gzip 8.56) | 46.76 КБ (gzip 8.56) |
| Модулей | 81 | 72 (скрипт) / 82 (десктоп) |

Скрипт похудел на 6 КБ: из графа ушли `adblock/index.ts`, `net-probe.ts`
и их зависимости. Десктоп подрос на 2 КБ за счёт двух файлов-переходников —
цена за то, что развилка решается на сборке, а не проверками в рантайме.
Тайпчек чистый, обе сборки собираются, живая проверка владельца пройдена.

### 7.7 Хвосты и точка остановки

- Ветка `desktop-dev-4`, вершина после этой записи — раздел 7 дописан поверх
  `5702e0a`.
- **Не проверено:** в браузерной сборке некому ставить класс `am-notr`
  на рекламные поддеревья (РИСК №4 из `AUDITION.md`). Если переводчик начнёт
  крутить очередь на контейнерах Venatus и Gamera, в `impl.noop.ts` придётся
  вернуть минимальный наблюдатель, который только помечает узлы и ничего
  не прячет.
- **Не сделано, согласовано как необязательное:** комментарий к `hideAds`
  в `src/core/settings.ts` всё ещё утверждает, что ключ работает везде —
  теперь это неправда. Туда же — вычистка отладочных `diagnose()`,
  `DIAG_SELECTOR`, `DIAG_LIMIT` и `diagReported` из `features/adblock/index.ts`.
- **Отступление от регламента.** По пункту 4 код правится локально, а через
  API идёт только журнал. В этот раз по прямой просьбе владельца все пять
  коммитов ушли через API, а он подтягивал их к себе `git pull --rebase`
  и проверял сборкой. Отступление разовое, договорённость не меняется.
