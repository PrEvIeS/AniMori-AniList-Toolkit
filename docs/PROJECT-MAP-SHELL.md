# Карта проекта, часть 3: оболочка, сборка, данные

Продолжение `PROJECT-MAP.md` и `PROJECT-MAP-MODULES.md`.

---

## 9. Оболочка Tauri (`src-tauri/`)

### Главная особенность

Окно открыто не на локальной странице, а на живом anilist.co. Приложение —
это браузер одного сайта со встроенной надстройкой, а не локальный фронтенд.
Отсюда три следствия:

1. окно создаётся кодом в `setup()`, а не в `app.windows` конфига (там пустой
   список): нужен внешний URL и скрипты инициализации;
2. разрешения выдаются окну на чужом домене, поэтому `capabilities/` держится
   минимальным;
3. внешние ссылки перехватываются в `on_navigation` и уходят в системный браузер,
   иначе окно приложения уехало бы на сторонний сайт без адресной строки
   и кнопки назад.

### lib.rs

- окно `main`, стартовый адрес — главная AniList, размер 1280×800, минимум 1024×600;
- бандл и стили вшиваются в двоичный файл через `include_str!` из `dist/`,
  поэтому `npm run build:tauri` обязателен до `cargo`;
- скрипт сетевой разведки выполняется только во вложенных кадрах и молчит,
  пока его не включат командой;
- команды: перезагрузка окна и открытие внешнего адреса (схема проверяется:
  только http и https);
- плагины: store, http, clipboard-manager, opener, dialog, window-state, updater;
- блокировщик ставится только под Windows; проверка обновлений запускается
  последней, чтобы не тормозить показ окна.

Положение и размер окна запоминаются между запусками (состояние окна:
размер, позиция, развёрнутость).

### Три места для новой команды

1. `#[tauri::command]` и `invoke_handler` в `lib.rs`;
2. `AppManifest::commands` в `build.rs`;
3. разрешение в `capabilities/default.json`.

Пропуск третьего пункта даёт отказ в рантайме без ошибки компиляции — самый
неприятный вариант.

### adblock.rs

Сетевой фильтр на событиях WebView2: запросы к рекламным хостам отбиваются
до выхода в сеть. Списки собраны вручную, без внешнего движка фильтров:
полноценный движок был в планах и остался открытым пунктом.

### tauri.conf.json

Идентификатор `com.foulnike.animori`; версия берётся из `package.json`, чтобы
номер не расходился; бандл только nsis; обновления тянутся из последнего
релиза GitHub по `latest.json` и проверяются по публичному ключу; установка
в тихом режиме.

Подпись обязательна: без переменных окружения с приватным ключом и паролем
сборка релиза не пройдёт, иначе автообновление отвергнет пакет.

---

## 10. Сборка (`vite.config.ts`)

Два режима различаются тремя вещами: алиасами, форматом вывода и плагином
юзерскрипта.

| | userscript | tauri |
|---|---|---|
| `@bridge-impl` | `MonkeyBridge.ts` | `TauriBridge.ts` |
| `@adblock-impl` | `impl.noop.ts` | `impl.desktop.ts` |
| Формат | сборка с шапкой юзерскрипта | IIFE, без динамических чанков |
| CSS | встраивается в скрипт | отдельный файл, вшивается в Rust |
| Очистка `dist` | да | нет (рядом лежит юзерскрипт) |

Минификация выключена намеренно: правила Greasy Fork требуют читаемого
исходника. Динамический `import()` запрещён: в юзерскрипте ему неоткуда
грузить чанки.

Номер версии в шапке юзерскрипта, в `tauri.conf.json` и в теге релиза идёт
из одного места — `package.json`.

Скрипты: `build` (тайпчек плюс юзерскрипт), `build:tauri`, `tauri:dev`,
`tauri:build`, `typecheck`, `format`. Неиспользованный импорт роняет тайпчек,
а значит и сборку.

Порядок в CI: сначала юзерскрипт, потом десктопный бандл, потом cargo.
После любой правки конфига сборки сверяется число модулей и размер вывода:
резкое изменение обычно означает, что в бандл уехало лишнее.

---

## 11. Данные

### Хранилище ключ-значение (через мост)

Группы ключей:

- авторизация и учётные данные: токен AniList, логин Shikimori;
- настройки с префиксом `set_`: перевод и его источники, виджеты, внешние
  ссылки и их домены, журнал, блокировка всплывающих окон плеера, адблок,
  видимость кнопок переноса и сравнения;
- пользовательские данные с префиксом `am_`: личный словарь, свои ссылки,
  избранные озвучки, акцент, сервис музыки;
- служебное: список игнора сравнения, записи журнала.

В браузере это хранилище Tampermonkey, в десктопе — файл в профиле пользователя.
Общего синхрона между сборками нет: это два независимых набора настроек.

### IndexedDB

`malCache` — ответы AniList по id; `shikiCache` — карточки Shikimori с меткой
времени. Срок годности 90 дней. Кэш чистится фоновым сборщиком и кнопкой
в настройках.

### Внешние сервисы

AniList GraphQL, Shikimori и его зеркала, Anime365, AnimeThemes, Kodik,
raw.githubusercontent для словаря. Ключ Kodik вшит в код — он публичный
и выдаётся сайту, а не пользователю.

---

## 12. Инварианты кодовой базы

Короткий список правил, нарушение которых уже приводило к дефектам.

1. Мост импортируется только как `@/bridge`, адблок — только как `@adblock-impl`.
2. Платформенный модуль выходит проверкой платформы на первой строке, а не
   разводит ветки по всему файлу.
3. Каждый сетевой запрос берёт слот у ограничителя темпа и явно задаёт
   `credentials`.
4. Молчаливый `catch` запрещён: либо обработка, либо запись в журнал.
5. Селекторы берутся из живого DOM, а не из предположений о разметке сайта.
6. Чужие узлы прячутся, а не удаляются.
7. Тумблер видимости не отменяет монтирование компонента.
8. Вложенные реактивные ссылки объявляются поверхностными.
9. Задачи смены роута идемпотентны.
10. Шапка юзерскрипта не расширяется новыми разрешениями без крайней
    необходимости: каждое право проверяется модерацией Greasy Fork.

---

## 13. Где что искать

| Задача | Файлы |
|---|---|
| Добавить настройку | `core/settings.ts`, `features/ui/settings-state.ts`, `SettingsModal.vue` |
| Добавить виджет тайтла | `features/media/`, регистрация в `main.ts` |
| Добавить кнопку | `features/ui/actions.ts`, `action-panel-state.ts` |
| Править перевод интерфейса | `features/translator/rules.ts`, `dictionary.json` |
| Править плеер | `features/media/player.ts` |
| Новая возможность оболочки | `src-tauri/src/lib.rs`, `build.rs`, `capabilities/` |
| Править списки рекламы | `features/adblock/index.ts`, `src-tauri/src/adblock.rs` |
| Разбор сбоев у пользователя | журнал в интерфейсе, `utils/logger.ts` |
